# 할부 (2026-09-24)

앞부분은 **지금 구현되어 있는 것**이고, 뒷부분은 **아직 정하지 않은 것**이다.

커밋은 둘이다. `3fffb0a 할부를 실적·청구·수수료로 나눠 다룬다` 가 뼈대를 세웠고,
그다음 커밋 "할부 이자를 전표 안에 담고, 달마다 나가는 돈으로 센다" 가 5.1(유이자 이자
계산)·5.2(회차 기준 보기)와 9장(이자를 전표 안으로)을 함께 담았다.

마이그레이션 `20260919170000_installment_interest`, `20260920100000_installment_shares`,
`20260922100000_installment_interest_shares`, `20260924100000_installment_interest_in_entry`
는 **로컬에만 적용했다.** 마지막 것을 적용하기 전에 `scripts/installment-interest-migrate.ts`
를 먼저 돌려야 하고(9장), 로컬에서는 2026-09-24 에 돌렸다 -- 옛 수수료 전표 5건을 지우고
유이자 할부 1건에 이자 176원을 얹었다. 배포 서버에는 어느 것도 올리지 않았다. 기기
사본의 스키마 번호는 23 이다(계획에 이자 칸이 붙었다).

---

## 1. 할부 하나를 네 가지가 다르게 센다

이 기능의 뼈대다. 한 결제를 두고 네 질문의 답이 서로 다르고, 그것이 원래 사실이다.

| 무엇 | 언제 얼마 | 어디 |
| --- | --- | --- |
| **지출·부채** | 산 날 **원금 + 이자** 전액 | 전표 한 건 (`buildExpense`) |
| **실적** | 결제한 주기에 이자를 뺀 결제액 | `creditPerformanceShares` (`packages/types/src/card-usage.ts`) |
| **청구** | 회차마다 그 회차의 원금 + 이자 | `billedShares` (`같은 파일`) |
| **월별 지출** | 회차가 서는 달마다 그 달 몫 | `installmentEntryViews` (`entry-rows.ts`) |

실적을 나누지 않는 까닭은 카드사가 세는 것이 승인 한 건이기 때문이다. 24개월로 나눠
갚는다고 그 달 실적이 1/24 만 오르지 않는다. 같은 까닭으로 **이자는 실적에서 뺀다** --
카드사가 혜택을 정할 때 세는 것은 승인된 결제액이지 수수료가 아니다.

원금 전표를 회차마다 만들지 않는 까닭은 **전표 합이 곧 부채이고 지출**이기 때문이다.
24건으로 쪼개면 둘 다 24배가 된다. 4장에서 다시 다룬다.

## 2. 화면

- **카드 상세 · 결제대금 탭**: 주기를 고르면 목록이 그 주기의 **청구 내역**으로 바뀐다.
  "24개월 중 3회차 100,000" 줄이 서고, 누르면 원 거래가 열린다.
  (`GET /cards/:id/billed-ledger`, 웹·앱 모두 `PeriodLedgerList`)
- **카드 상세 · 거래내역**: 계좌 원장이라 산 날 한 줄이고, 금액은 원금 + 이자 전액이다.
  그 줄의 합이 곧 카드에 갚을 돈(남은 대금)이다.
- **거래 상세**: 할부 줄에 "3개월 할부 · 유이자 · 이자 6,000 포함"으로 적는다
  (`installmentLabel`). 금액이 산 값보다 큰 까닭이 그 한 마디에 있다.
- **막대그래프**: 제자리는 오늘을 품은 주기가 맨 오른쪽이다(`cardUsageAnchorEnd`,
  `packages/core/src/lib/card-usage-chart.ts:158`). 앞 주기는 왼쪽으로 끌면 나온다.
- **편집기**: 개월수를 고르면 **무이자·유이자**를 반드시 고르게 한다. 기본값을 두지
  않는 까닭은 어느 쪽으로 두어도 한쪽이 조용히 틀리기 때문이다. 그 아래 **회차 금액**
  칸이 기본 분할을 채워 보여 주고, 명세서와 다르면 고쳐 적는다. 유이자면 **회차 이자**
  칸이 따라 서고, 그 아래 "갚을 돈 {금액 + 이자}"를 적어 둔다 -- 금액 칸은 산 값
  그대로 두고 여기서만 밝힌다.

## 3. 저장되는 것과 계산되는 것

```
InstallmentPlan
  totalMonths       개월수
  interestBearing   이자가 붙는 할부인가
  principalShares   사람이 적어 둔 회차별 원금 (JSON 배열, 없으면 개월수로 나눈다)
  interestShares    사람이 적어 둔 회차별 이자 (JSON 배열, 이 합은 전표 금액 안에 있다)
  monthlyPayment    고정형의 월 납입액 (표를 다시 계산하는 입력)
  annualRate        변동형의 연이율
```

- **전표 금액 = 원금 + 이자.** 조립(`buildExpense`)이 이자 합을 분류 줄에 비례로 얹고
  카드 다리를 그만큼 더 빚지게 한다. 이자의 분류는 거래의 분류를 따라간다 -- 한
  결제에서 나온 돈이라 따로 세면 "그 달에 식비로 얼마 나갔나"의 답이 갈린다.
- 회차 원금의 합은 **금액에서 이자를 뺀 값**과 같아야 한다(`installmentSharesOf`).
  이자까지 더해 적으면 원금이 부풀어 실적에서 이자를 덜어낼 수 없다.
- 회차 금액은 `principalShares` 가 있으면 그것이 사실이고, 없으면 `installmentPrincipals`
  가 나눈다. 끝수를 어디에 붙이는지는 카드사마다 달라 계산만으로는 명세서와 맞출 수
  없다 — 1,000원 3개월이 334/333/333 일 수도 334/334/332 일 수도 있다.
- 적어 둔 값은 **개수와 합만** 검사한다. 금액이나 개월수를 고치면 버리고 다시 나눈다.
- **외화가 얽힌 결제에는 유이자 할부를 적을 수 없다**(`INSTALLMENT_INTEREST_CURRENCY`).
  이자는 명세서에 찍힌 금액 그대로 적는 값이라 환산할 그때의 환율이 계획에 없다. 원화
  카드로 한 외화 결제도 막는다 -- 이자가 섞인 환산액에서 환율을 되짚으면(`deriveRate`)
  적은 적 없는 환율이 화면에 뜬다.

  폼에서 이 판단은 **장부 통화와 견준다.** 웹 편집기의 통화 칸은 비는 법이 없어
  (원화 결제에도 'KRW' 가 들어 있다) 빈 값으로 보면 모든 유이자 할부가 막힌다. 같은
  자리에 있던 회차 합계·월 납입액 검사도 그 때문에 영영 돌지 않고 있었다.
- 저장할 때는 **화면에 보이는 회차 이자를 그대로 싣는다.** 계산으로 채워 준 기본값도
  손대지 않았다고 버리지 않는다 -- 이자는 전표 금액에 들어가는 값이라, 싣지 않으면 고쳐
  저장하는 순간 갚을 돈이 원금만 남고 서버에는 되살릴 자리가 없다(서버는 이자를 계산하지
  않는다). 방식을 고르지 않아 칸이 비어 있으면 아무것도 싣지 않는다 -- "아직 모른다"와
  "이자가 없다"는 다르다.
- 개월수를 바꾸면 회차 원금·회차 이자와 함께 **월 납입액도 비운다.** 개월수와 한 쌍인
  값이라("10,000원을 3개월, 매달 4,000원") 남겨 두면 표가 풀리지 않아 저장이 막힌다.
  연이율은 개월수와 무관해 그대로 둔다.
- 고치려고 열 때 폼은 **이자를 되뺀다**. 금액 칸은 산 값이고, 그래야 회차 원금의 합과
  어긋나지 않는다. 분할이면 줄마다 같은 비율로 되뺀다. 되빼는 함수는 하나다
  (`interestInAmount`·`withoutInterest`) -- 앱은 `entryFormFromItem` 이, 웹 편집기는
  자기 상태를 채우는 자리에서 그것을 부른다. 두 벌로 두면 같은 거래를 웹에서 열 때와
  앱에서 열 때 금액 칸이 갈린다.
- 쓰이지 않던 `InstallmentCharge` 표와 `InstallmentPlan.feeAmount` 는 지웠다. 회차 금액을
  표로 저장하면 마감일을 바꿨을 때 다시 계산되는 값과 갈린다(설계 문서의 D7).

## 4. 논의: 매달 나갈 거래를 미리 등록하면?

사용자의 제안이다. "할부를 만들면 거래내역에 매달 나갈 거래가 미리 다 등록되어 있어야
월별 지출에서 확인할 수 있다."

목적은 맞다. 다만 **전표를 미리 만드는 방식**에는 여섯 가지가 걸린다.

1. **오늘 잔액이 틀어진다.** 잔액은 전표 합계이고 날짜를 보지 않는다. 저장하는 순간
   더한다(`LedgerService.applyBalanceDeltas`). 미래 23건을 만들면 그 돈이 오늘 이미
   빠져나간 것이 된다.
2. **카드 빚이 거꾸로 기록된다.** 2,400만원 할부면 카드사에 진 빚은 오늘 2,400만원인데,
   매달 100만원짜리 전표로 두면 빚이 조금씩 생겨나는 것이 되어 남은 대금·청구액이
   명세서와 맞지 않는다.
3. **실적이 다시 나뉜다.** 실적은 전표에서 나오므로 1장의 규칙이 깨진다.
4. **고칠 때마다 24건을 함께 손봐야 한다.** 취소·환불·선결제·금액 수정이 전부 그렇다.
5. **목록이 덮인다.** 24개월 할부 두 건이면 거래내역에 48줄이 미리 선다.
6. **동기화도 24배다.** 오프라인에서 적으면 명령 24개가 큐에 쌓인다.

## 5. 그래서 무엇을 만들까 (아직 안 만든 것)

목적(월별 지출에서 보이기)은 전표를 쪼개지 않고도 이룰 수 있다. 셋을 제안했고
어느 것부터 할지는 정하지 않았다.

### 5.1 유이자 할부의 회차별 이자 — **만들었다 (2026-09-22)**

카드사가 이자를 정하는 방식이 둘이고, 편집기에서 어느 쪽인지 고른다.

- **고정형**(매달 같은 금액): 월 납입액을 적으면 월이율을 되짚어 상환표를 만든다.
  낸 돈에서 이자를 먼저 떼고 남은 것이 원금이라 앞 회차일수록 이자가 크다.
  10,000원 3개월에 매달 4,000원이면 970/676/354 원이 이자다(`fixedPaymentSchedule`).
- **변동형**(연이율): 원금은 무이자처럼 고르게 나누고, 이자는 그때 남아 있는 원금에
  월할로 붙는다. 뒤로 갈수록 가벼워진다(`annualRateSchedule`).
- **직접 적기**: 방식을 고르지 않으면 이자 칸이 빈다. 0 으로 채우지 않는 까닭은
  "아직 모른다"와 "이자가 없다"가 구별되어야 하기 때문이다.

계산은 **기본값을 채워 주는 데까지**다. 카드사마다 올림하는 자리가 달라 명세서와 다르면
회차마다 고쳐 적고, 고친 값이 계획에 남는다(`InstallmentPlan.interestShares`). 무이자로
되돌리면 이자 쪽(회차 이자·월 납입액·연이율)을 통째로 비운다.

**이자는 전표 금액에 들어간다** (9장). 처음에는 회차마다 사람이 적는 수수료 전표로
두었지만, 그러면 산 날에 카드 빚이 원금만큼만 잡히고 매달 갚을 돈이 적히기 전까지
어디에도 없었다.

검사: `core/scripts/installment-schedule-smoke.ts`(상환표),
`core/scripts/entry-form-smoke.ts`(폼 검증과 짐).

### 5.2 회차 기준 보기 — **만들었다 (2026-09-22)**

거래 화면의 더보기에 스위치를 두었다. **기본은 회차 기준이다** (9장에서 바꿨다).
발생 기준은 "언제 샀나"를 묻는 화면 -- 이를테면 그 달 카드값을 명세서와 대조할 때 --
을 위해 남겨 두었다.

- **발생 기준**(기본): 산 달에 2,400,000원.
- **회차 기준**: 그 달에 청구되는 100,000원과 그 회차의 이자. 합계도 목록도 함께 나뉜다.

**회차가 서는 달은 구매 달부터 달력 월로 한 달씩이다.** 카드 마감 주기가 아니다 --
카드 지출은 쓴 달에 잡히고 다음 달 결제대금은 지출이 아닌데, 회차만 결제일 달로 옮기면
같은 카드의 두 거래가 다른 규칙을 따른다. 마감일이 말일이 아닌 카드에서는 카드 상세의
청구 내역(`billedShares`)과 한 달 어긋날 수 있다. 그쪽은 명세서를 맞추는 화면이고
이쪽은 생활비를 세는 화면이라 쓰임이 다르다.

| 어디 | 무엇이 달라지는가 |
| --- | --- |
| 월별 합계·분류·수단·일별 (`basis=installment`) | 할부 줄을 회차로 펴고 구간 밖은 버린다 |
| 거래 목록 | 그 구간에 선 회차만 한 줄, 금액은 원금 + 이자 |
| 날짜별 줄과 달력 | 회차가 **그 달의 같은 날**로 옮겨져 선다 |
| 지난달에 산 할부 | `GET /entries/installment-rows` 로 따로 실어 목록에 세운다 |

구현의 뼈대는 셋이다.

- `installmentMonthShares`·`installmentLineShares` (`types/src/installment-schedule.ts`):
  회차가 서는 달과 그 달의 원금·이자. 분할 거래는 줄 금액의 비율로 다시 나눈다.
- `expandInstallmentRows` (`types/src/report-aggregation.ts`): 집계 줄을 회차로 편다.
  이자는 줄 금액 안에 이미 들어 있다.
- `installmentEntryViews` (`types/src/entry-rows.ts`): **회차 기준의 입구 하나.** 금액과
  날짜를 그 회차의 것으로 옮기고 회차 번호를 실어 준다. 목록(`entryRows`)·날짜별 합계·
  달력·수단별이 모두 이 한 함수를 거친다 -- 두 곳에서 옮기면 한 화면 안에서 숫자가
  갈리고, 두 번 거치면 회차를 또 쪼갠다(그 자리는 함수가 막는다).

**눌러서 여는 것은 옮기기 전의 거래다.** 옮긴 거래는 `origin` 에 원래의 것을 달고 다니고,
자세히 보기와 고치기는 `originalEntry` 로 그것을 연다. 목록에 선 것은 그 달의 회차 몫
이지만 사용자가 적은 것은 산 날의 전액이라, 1회차를 눌렀는데 폼이 1회차 금액을 들면
그대로 저장하는 순간 24개월 할부가 한 달치 금액으로 바뀐다. 자산 탭은 줄에서 전표 id 만
받아 다시 읽으므로(`getEntry`) 처음부터 적은 그대로다.

지난 할부를 싣는 조회(`GET /entries/installment-rows`)는 **목록 질의와 같은 방식으로**
구간을 읽는다 -- 달 이름 하나든 인스턴트 두 개든 된다. 거래 화면은 달을 볼 때 이름만
보내므로(`listRangeOf`), 인스턴트만 받던 때에는 이 조회가 늘 비어 산 달의 1회차만 서고
나머지 회차는 합계에만 잡혔다.

질의는 **앞으로 60개월 넓혀 읽고 편 뒤에 구간 밖을 버린다**(`INSTALLMENT_LOOKBACK_MONTHS`).
지난달에 산 할부의 회차가 이번 달에 서기 때문이고, 조건을 아예 지우면 원장을 통째로
읽게 된다. 서버와 기기 사본이 같은 차례를 따른다.

외화로 청구되는 카드에는 유이자 할부를 적을 수 없다(3장). 회차 원금은 **비율로만** 쓰여
통화가 갈려도 제대로 나뉘지만, 이자는 금액 그대로 더하는 값이라 환산할 그때의 환율이
계획에 없다.

검사: `api/scripts/installment-basis-smoke.ts`,
`core/scripts/installment-basis-mirror-smoke.ts`(사본이 같은 값을 내는가).

### 5.3 "이번 달 나갈 돈" (현금 흐름) — 아직 안 만들었다

월간 지출은 발생 기준이라 할부 회차도 대출 원금 상환도 들어가지 않는데, 통장에서는
매달 빠져나간다. 카드 청구액(할부 회차 포함) + 대출 상환 예정 + 정기 자동이체를 모아
한 자리에서 본다. 홈·가계 중 어디에 둘지는 정하지 않았다.

## 6. 대출과의 관계

"대출은 매달 지출에 나오는데 할부는 안 나온다"는 물음에서 시작했다. 두 가지는 이미 같은
규칙으로 돌고 있다 — **지출은 돈을 쓴 시점, 상환은 지출이 아님**. 대출 쪽이 달라 보이는
까닭은 빌린 돈으로 무엇을 샀는지가 장부 밖(기초잔액)에 있어서다. 이 가계부의 대출 계좌는
기초잔액 1,000,000원으로 들어왔고, 실제 상환 전표는 원금(부채 감소)과 이자(지출)로 이미
갈라 적혀 있다.

원금까지 지출로 적으면 그때는 정말 어긋난다. 같은 돈이 지출로도 세이고 빚도 줄지 않아
대출 잔액이 영영 그대로 남는다.

5.2 와 5.3 을 만들면 두 가지가 나란히 선다. 회차 기준으로 보면 할부도 대출 상환도
"매달 나가는 돈"으로 들어오고, 발생 기준으로 보면 둘 다 빚을 갚는 것이라 빠진다.

## 7. 지금 있는 검사

| 스크립트 | 보는 것 |
| --- | --- |
| `api/scripts/card-billed-ledger-smoke.ts` | 청구 내역의 회차 줄, 합계 = 그래프 막대, 회차 금액 직접 입력 |
| `api/scripts/installment-interest-smoke.ts` | 이자가 전표·부채·청구에 들고 실적에서는 빠지는가, 외화 카드 거부 |
| `api/scripts/card-performance-ledger-smoke.ts` | 실적은 결제한 주기에 전액 |
| `core/scripts/card-billed-mirror-smoke.ts` | 기기 사본이 서버와 같은 줄을 내는가 (유이자 포함) |
| `core/scripts/card-usage-window-smoke.ts` | 막대 창의 제자리와 끌기 |
| `core/scripts/entry-form-smoke.ts` | 폼 검증 (유이자 선택 강제, 회차 합·개수, 빈 칸), 고칠 때 이자 되빼기 |
| `api/scripts/installment-basis-smoke.ts` | 회차 기준의 월 합계·분류·수단·목록·**예산** |
| `core/scripts/local-store-smoke.ts` | 서버 응답을 떠서 사본과 견주기 (청구 내역 포함) |
| `core/scripts/installment-schedule-smoke.ts` | 고정형·변동형 상환표, 원금 합, 달력 월로 펴기 |
| `core/scripts/installment-basis-mirror-smoke.ts` | 오프라인에서도 같은 값이 나오는가 (예산 포함) |

## 8. 아직 남은 것 (2026-09-24)

- **스위치는 기억하지 않는다.** 거래 화면을 다시 열면 회차 기준(기본)으로 돌아온다.
  기억하려면 어디에 둘지(기기/프로젝트)를 정해야 한다.
- 분할 거래의 이자는 줄 금액의 비율로 나누고 회차마다 반올림한다. 줄 합이 1원 어긋날
  수 있는 자리다. 고쳐 저장할 때 되빼는 쪽도 같은 비율이라, 드문 반올림 경계에서 줄
  하나가 1원 옮겨갈 수 있다.
- 5.3("이번 달 나갈 돈")은 그대로 남아 있다.

## 9. 이자를 전표 안으로 (2026-09-24)

사용자가 고른 바다. "수수료 전표는 없애고 전표 금액에 이자 포함, 달력 월 기준 유지."

**무엇이 문제였나.** 이자를 회차마다 사람이 적는 별도의 전표로 두니, 산 날에는 카드
빚이 원금만큼만 잡히고 매달 갚을 돈은 적히기 전까지 어디에도 없었다. 거래내역은 산 달에
24개월치 전액을 한 줄로 보여 주고, 가계의 분류별·수단별도 그 한 달에 몰렸다.

**무엇을 바꿨나.**

1. **조립이 이자를 얹는다.** 회차 이자의 합이 분류 줄에 비례로 들어가고 카드 다리가
   그만큼 더 빚진다. 전표 금액이 곧 카드에 갚을 돈이다.
2. **실적은 이자를 뺀다.** `creditPerformanceShares` 가 다리 금액에서 이자 합을 덜어낸다.
3. **청구는 회차마다 원금 + 이자.** `billedShares` 가 회차 원금에 그 회차의 이자를
   더한다. 결제대금 탭의 그래프와 목록이 함께 달라진다.
4. **거래내역과 가계가 회차 기준이 기본.** 거래 화면의 스위치 기본값을 바꾸고, 가계
   화면(웹 `/dashboard`, 앱 `LedgerScreen`)은 `basis=installment` 를 실어 보낸다.
   날짜별 줄·달력도 회차 날짜로 선다.
4-1. **예산도 회차 기준이다.** 화면이 고르는 값이 아니라 늘 그렇다 -- 예산은 "이 달에
   이만큼까지 쓴다"는 약속이라, 24개월 할부를 산 달에 전액으로 세면 그 달 하나가 통째로
   터지고 남은 스물세 달에는 나갈 돈이 진행률에 잡히지 않는다. 서버와 기기 사본이 같다
   (`budgets.service`, `local-home-port` 의 `getBudgetForMonth`). 질의가 해야 하는 일
   (할부만 앞으로 넓혀 읽고, 편 뒤에 창 밖을 버리기)은 리포트와 한 곳에서 나눠 쓴다
   (`api/src/common/installment-scope.ts`).
5. **수수료 입력 칸을 없앴다.** `PendingFeePanel`, `GET|PATCH /cards/:id/pending-fees`,
   `JournalEntry.installmentPlanId`·`installmentSequence` 가 함께 사라졌다.

**옮겨 심기.** 이미 있는 데이터는 두 가지가 어긋난다 -- 옛 수수료 전표가 남아 있고,
유이자 할부의 전표에는 이자가 들어 있지 않다. `api/scripts/installment-interest-migrate.ts`
가 둘을 한꺼번에 처리한다(보기만 하는 것이 기본, `--apply` 로 고친다). **마이그레이션
`20260924100000_installment_interest_in_entry` 를 적용하기 전에 돌려야 한다** -- 그 칸이
사라지면 옛 수수료 전표를 가려낼 길이 없다.

배포 서버에 올릴 때도 차례가 같다. 돌리기 전에 덤프를 받아 두면 되돌릴 자리가 남는다
(로컬에서는 `backups/money_db_before_installment_interest_20260924.sql` 을 받아 두었고,
`scripts/check-balances.ts` 로 잔액이 다리 합계와 그대로 맞물리는지 확인했다 -- 예전부터
있던 어긋남 두 건 말고는 새로 생긴 것이 없다).
