# 할부 (2026-09-20)

앞부분은 **지금 구현되어 있는 것**이고, 뒷부분은 **아직 정하지 않은 것**이다.
다음에 이 문서를 가져와서 요청하면 5장부터 이어서 만들면 된다.

커밋은 `3fffb0a 할부를 실적·청구·수수료로 나눠 다룬다` 한 개다.
마이그레이션 `20260919170000_installment_interest`, `20260920100000_installment_shares` 는
로컬에만 적용했다. 배포 서버에는 아직 올리지 않았다.

---

## 1. 할부 하나를 세 가지가 다르게 센다

이 기능의 뼈대다. 한 결제를 두고 세 질문의 답이 서로 다르고, 그것이 원래 사실이다.

| 무엇 | 언제 얼마 | 어디 |
| --- | --- | --- |
| **지출·부채** | 산 날 전액 | 전표 한 건 (`buildExpense`) |
| **실적** | 결제한 주기에 전액 | `creditPerformanceShares` (`packages/types/src/card-usage.ts:114`) |
| **청구** | 회차마다 나뉘어 뒤 주기로 | `billedShares` (`같은 파일:173`) |
| **수수료** | 회차마다 사람이 명세서를 보고 | 수수료 전표 (`CardLedgerService.settleFees`) |

실적을 나누지 않는 까닭은 카드사가 세는 것이 승인 한 건이기 때문이다. 24개월로 나눠
갚는다고 그 달 실적이 1/24 만 오르지 않는다.

원금 전표를 회차마다 만들지 않는 까닭은 **전표 합이 곧 부채이고 지출**이기 때문이다.
24건으로 쪼개면 둘 다 24배가 된다. 4장에서 다시 다룬다.

## 2. 화면

- **카드 상세 · 결제대금 탭**: 주기를 고르면 목록이 그 주기의 **청구 내역**으로 바뀐다.
  "24개월 중 3회차 100,000" 줄이 서고, 누르면 원 거래가 열린다.
  (`GET /cards/:id/billed-ledger`, 웹·앱 모두 `PeriodLedgerList`)
- **카드 상세 · 수수료 칸**: 마감된 유이자 회차가 **거래별로 묶여** 뜬다. 명세서 금액을
  적으면 수수료 전표가 생긴다. (`PendingFeePanel`, `GET|PATCH /cards/:id/pending-fees`)
- **막대그래프**: 제자리는 오늘을 품은 주기가 맨 오른쪽이다(`cardUsageAnchorEnd`,
  `packages/core/src/lib/card-usage-chart.ts:158`). 앞 주기는 왼쪽으로 끌면 나온다.
- **편집기**: 개월수를 고르면 **무이자·유이자**를 반드시 고르게 한다. 기본값을 두지
  않는 까닭은 어느 쪽으로 두어도 한쪽이 조용히 틀리기 때문이다. 그 아래 **회차 금액**
  칸이 기본 분할을 채워 보여 주고, 명세서와 다르면 고쳐 적는다.

## 3. 저장되는 것과 계산되는 것

```
InstallmentPlan
  totalMonths       개월수
  interestBearing   수수료가 붙는 할부인가
  principalShares   사람이 적어 둔 회차별 원금 (JSON 배열, 없으면 개월수로 나눈다)

JournalEntry
  installmentPlanId, installmentSequence   수수료 전표가 메우는 회차
```

- 회차 금액은 `principalShares` 가 있으면 그것이 사실이고, 없으면 `installmentPrincipals`
  가 나눈다(`card-usage.ts:407`). 끝수를 어디에 붙이는지는 카드사마다 달라 계산만으로는
  명세서와 맞출 수 없다 — 1,000원 3개월이 334/333/333 일 수도 334/334/332 일 수도 있다.
- 적어 둔 값은 **개수와 합만** 검사한다(`installmentSharesOf`, `entry-build.ts:1075`).
  금액이나 개월수를 고치면 버리고 다시 나눈다.
- 수수료 전표의 날짜는 결제일이 아니라 그 회차 주기의 **마감일**이다. 결제일로 두면
  다음 주기 청구서로 넘어가 명세서와 한 달 밀린다.
- 원 거래를 고치면 계획이 cascade 로 사라지므로 `replaceEntry` 가 같은 id 로 다시 만들고
  수수료 전표의 연결을 도로 맺는다. 그러지 않으면 적은 회차가 다시 떠올라 두 번 적게 된다.
- 쓰이지 않던 `InstallmentCharge` 표와 `InstallmentPlan.feeAmount` 는 지웠다. 회차 금액을
  표로 저장하면 마감일을 바꿨을 때 다시 계산되는 값과 갈린다(설계 문서의 D7).

## 4. 논의: 매달 나갈 거래를 미리 등록하면?

사용자의 제안이다. "할부를 만들면 거래내역에 매달 나갈 거래가 미리 다 등록되어 있어야
월별 지출에서 확인할 수 있다."

목적은 맞다. 다만 **전표를 미리 만드는 방식**에는 여섯 가지가 걸린다.

1. **오늘 잔액이 틀어진다.** 잔액은 전표 합계이고 날짜를 보지 않는다. 저장하는 순간
   더한다(`LedgerService.applyBalanceDeltas`). 미래 23건을 만들면 그 돈이 오늘 이미
   빠져나간 것이 된다.
2. **카드 빚이 거꾸로 기록된다.** 2,400만원 할부면 카드사에 진 빚은 오늘 2,400만원인데,
   매달 100만원짜리 전표로 두면 빚이 조금씩 생겨나는 것이 되어 남은 대금·청구액이
   명세서와 맞지 않는다.
3. **실적이 다시 나뉜다.** 실적은 전표에서 나오므로 1장의 규칙이 깨진다.
4. **고칠 때마다 24건을 함께 손봐야 한다.** 취소·환불·선결제·금액 수정이 전부 그렇다.
5. **목록이 덮인다.** 24개월 할부 두 건이면 거래내역에 48줄이 미리 선다.
6. **동기화도 24배다.** 오프라인에서 적으면 명령 24개가 큐에 쌓인다.

## 5. 그래서 무엇을 만들까 (아직 안 만든 것)

목적(월별 지출에서 보이기)은 전표를 쪼개지 않고도 이룰 수 있다. 셋을 제안했고
어느 것부터 할지는 정하지 않았다.

### 5.1 월 납입액으로 받기 (고정 이자 할부)

할부는 두 가지다. 매달 같은 금액이 나가는 것(이자 고정)과 달마다 다른 것(이자 변동).
둘은 다른 구조가 아니라 **수수료를 언제 정하느냐**의 차이다.

- **고정**: 살 때 "월 납입액 × 개월수"를 받는다. 원금 회차는 이미 알고 있으므로
  수수료 = 월 납입액 − 원금 회차다. 회차 주기가 마감되면 **자동으로** 수수료 전표를
  만든다. 사람이 매달 적지 않는다.
- **변동**: 지금 그대로. 마감된 회차가 카드 상세에 떠오르고 명세서를 보고 적는다.

수수료 전표를 미리 24건 만들어 두지는 않는다. 4장의 1·2번과 같은 이유로, 미래 수수료가
오늘 빚에 섞인다.

### 5.2 월별 지출의 "회차 기준 보기"

월간 합계와 분류별 리포트에 스위치를 하나 둔다.

- **발생 기준**(기본, 지금): 산 달에 2,400,000원.
- **회차 기준**: 그 달에 청구되는 100,000원씩. 분류 합계도 함께 나뉘어, 식비 3개월
  할부면 식비가 매달 1/3 씩 선다.

재료는 이미 있다. `billedShares` 가 회차 금액과 그 회차가 청구되는 주기를 낸다. 월간
집계에서 카드 할부 줄만 그 회차대로 달에 배분하면 되고, 서버와 기기 사본이 같은 함수를
쓰므로 웹·앱이 저절로 같아진다. 기본을 발생 기준으로 두는 까닭은 보고 있던 숫자가
말없이 바뀌면 안 되기 때문이다.

### 5.3 "이번 달 나갈 돈" (현금 흐름)

월간 지출은 발생 기준이라 할부 회차도 대출 원금 상환도 들어가지 않는데, 통장에서는
매달 빠져나간다. 카드 청구액(할부 회차 포함) + 대출 상환 예정 + 정기 자동이체를 모아
한 자리에서 본다. 홈·가계 중 어디에 둘지는 정하지 않았다.

## 6. 대출과의 관계

"대출은 매달 지출에 나오는데 할부는 안 나온다"는 물음에서 시작했다. 두 가지는 이미 같은
규칙으로 돌고 있다 — **지출은 돈을 쓴 시점, 상환은 지출이 아님**. 대출 쪽이 달라 보이는
까닭은 빌린 돈으로 무엇을 샀는지가 장부 밖(기초잔액)에 있어서다. 이 가계부의 대출 계좌는
기초잔액 1,000,000원으로 들어왔고, 실제 상환 전표는 원금(부채 감소)과 이자(지출)로 이미
갈라 적혀 있다.

원금까지 지출로 적으면 그때는 정말 어긋난다. 같은 돈이 지출로도 세이고 빚도 줄지 않아
대출 잔액이 영영 그대로 남는다.

5.2 와 5.3 을 만들면 두 가지가 나란히 선다. 회차 기준으로 보면 할부도 대출 상환도
"매달 나가는 돈"으로 들어오고, 발생 기준으로 보면 둘 다 빚을 갚는 것이라 빠진다.

## 7. 지금 있는 검사

| 스크립트 | 보는 것 |
| --- | --- |
| `api/scripts/card-billed-ledger-smoke.ts` | 청구 내역의 회차 줄, 합계 = 그래프 막대, 회차 금액 직접 입력 |
| `api/scripts/installment-fee-smoke.ts` | 수수료 한살이 (떠오름·적음·중복 거부·원 거래 수정) |
| `api/scripts/card-performance-ledger-smoke.ts` | 실적은 결제한 주기에 전액 |
| `core/scripts/card-billed-mirror-smoke.ts` | 기기 사본이 서버와 같은 줄을 내는가 |
| `core/scripts/pending-fees-smoke.ts` | 거래별 묶음과 이름 규칙 |
| `core/scripts/card-usage-window-smoke.ts` | 막대 창의 제자리와 끌기 |
| `core/scripts/entry-form-smoke.ts` | 폼 검증 (유이자 선택 강제, 회차 합·개수, 빈 칸) |
| `core/scripts/local-store-smoke.ts` | 서버 응답을 떠서 사본과 견주기 (청구 내역 포함) |
