# 보관함 (아직 거래가 아닌 후보) 구현 기록 — 2026-09-08 ~ 09

거래 화면 머리글에 **보관함**을 두었다. 탭이 셋이다.

- **알림 등록용** — 이 기기로 오는 결제·입금 알림을 읽어 만든 후보.
- **캡처 인식용** — 사용자가 올린 화면 캡처에서 글자를 읽어 만든 후보.
- **반복 등록** — 사람이 미리 적어 둔 일정(매월 25일, 3일마다 …)에서 그 날이 되면
  스스로 만들어지는 후보.

둘 다 **거래가 아니다.** 합계·잔액·예산은 이 목록을 보지 않는다. 사용자가 후보의
"거래로 적기"를 눌러 값이 채워진 거래 추가 팝업에서 저장하는 순간에 비로소 전표가
만들어지고, 그 뒤에 후보에 등록 표시가 남는다.

**캡처는 웹과 앱 둘 다에서 읽는다** — 앱은 기기 OCR(ML Kit), 웹은 브라우저 OCR
(tesseract.js)이다. 어느 쪽에서도 사진은 서버로 가지 않는다. 알림 읽기는 안드로이드만
할 수 있어 앱이 맡는다.

---

## 1. 어디에 무엇이 있는가

### 서버 (`packages/api`)

| 파일 | 하는 일 |
| --- | --- |
| `prisma/schema.prisma` 의 `EntryDraft` | 후보 한 줄. 읽어 낸 값 전부가 nullable 이다 |
| `prisma/migrations/20260908120000_entry_draft/` | 표·색인·동기화 트리거 |
| `src/modules/entry-drafts/` | 목록·담기(여러 건)·손질·등록 표시·지우기·정리 |
| `src/modules/sync/sync.service.ts` | 변경 피드에 `entryDrafts` 를 실어 보낸다 |
| `prisma/schema.prisma` 의 `RecurringRule` | 반복 한 줄. 날짜는 **TEXT "YYYY-MM-DD"** |
| `prisma/migrations/20260909100000_recurring_rule/` | 반복 표 + `EntryDraft.recurringRuleId` |
| `src/modules/entry-drafts/recurring.service.ts` | 규칙의 목록·손질 (회차는 만들지 않는다) |

엔드포인트는 후보를 담고 꺼내는 `/entry-drafts` 와 규칙을 담고 꺼내는
`/recurring-rules` 둘이다. **회차를 만드는 엔드포인트는 없다** -- 기기가 규칙을 읽어
밀린 날을 셈해 `/entry-drafts` 로 올리고, 서버는 그 날이 규칙과 오늘에 맞는지만 본다
(3.18).

거래도 마찬가지다. **거래를 만드는 엔드포인트는 두지 않았다** —
등록은 기존 거래 생성 경로(`POST /entries`, 오프라인이면 아웃박스 명령)를 그대로
쓰고 여기에는 `PATCH` 로 표시만 남긴다. 전표의 검증과 잔액 반영이 한 자리에 남아야
한다.

### 공용 (`packages/core`)

| 파일 | 하는 일 |
| --- | --- |
| `lib/draft-parse.ts` | **문구 → 값.** 알림과 캡처가 같은 규칙을 지난다 |
| `lib/draft-match.ts` | 읽은 값을 이 가계부의 카드·통장·분류와 맞춘다 |
| `lib/draft-collect.ts` | 읽은 값 → 담을 모양. **앱과 웹이 함께 쓴다** |
| `data/draft-port.ts` | 보관함 창구 (웹은 서버, 앱은 사본). **담기도 여기로 간다** |
| `data/local-draft-port.ts` | 사본에서 읽고, 처리는 대기 큐를 지난다 |
| `hooks/useEntryDrafts.ts` | 목록·건수·등록/무시/지우기 |
| `data/entry-form.ts` 의 `entryFormFromDraft` | 후보 → 입력 폼 값 |
| `data/schema.ts` | 사본의 `entry_draft`·`draft_op` 표 (SCHEMA_VERSION 13) |
| `hooks/useRecurringRules.ts` | 반복 목록·손질. **읽은 뒤 밀린 회차를 담는다** |
| `lib/recurring-drafts.ts` | 반복 규칙 → 담을 후보 (웹·앱이 함께 쓴다) |
| `scripts/recurring-smoke.ts` | 일정 셈(`dueOccurrences`)을 20여 갈래로 검사 |
| `scripts/draft-parse-smoke.ts` | 실제 문구 40여 건으로 파서를 검사 |
| `scripts/inbox-mirror-smoke.ts` | 사본·대기 큐·자리표를 검사 |

일정 셈은 `packages/types/src/recurring.ts` 에 있다. `dueOccurrences`·`nextOccurrence`·
`checkRecurring` 은 **서버와 두 화면이 같은 함수를 부른다** -- 다음 예정일을 서버가
셈해 보내고 화면이 검사만 다시 하는 식이면, 두 셈이 어긋난 날 하루가 조용히 밀린다.

### 앱 (`packages/app`)

| 파일 | 하는 일 |
| --- | --- |
| `modules/inbox-native/` | **로컬 Expo 모듈.** 알림 리스너 + 기기 OCR (Kotlin) |
| `src/inbox.ts` | 버퍼 비우기 → 파싱 → 맞춤 → 서버에 담기 |
| `src/screens/InboxScreen.tsx` | 보관함 화면 (`/transactions/inbox`) |
| `src/components/RecurringRuleModal.tsx` | 반복을 만들고 고치는 팝업 |
| `src/components/FormFields.tsx` | 거래 폼과 반복 폼이 함께 쓰는 `Field`·`Chips` |
| `src/screens/TransactionsScreen.tsx` | 머리글의 보관함 아이콘 + 대기 건수 |

### 웹 (`packages/web`)

| 파일 | 하는 일 |
| --- | --- |
| `src/app/transactions/inbox/page.tsx` | 보관함 화면 + 캡처 읽기 + 반복 목록 |
| `src/components/RecurringRuleModal.tsx` | 반복을 만들고 고치는 팝업 |
| `src/components/CaptureDropzone.tsx` | 끌어다 놓기·눌러 고르기·붙여넣기 |
| `src/lib/capture-ocr.ts` | 브라우저 OCR (tesseract.js) + 줄 되돌리기 |
| `src/app/transactions/page.tsx` | 머리글의 보관함 아이콘 + 대기 건수 |
| `src/components/EntryEditor.tsx` | `openDraft` 로 후보를 폼에 담는다 |

---

## 2. 값이 흐르는 길

```
[알림]     안드로이드 알림 → NotificationListenerService → 기기 버퍼(SharedPreferences)
                                                          ↓ 앱이 열릴 때 / "지금 확인하기"
[캡처·앱]  고른 사진 → ML Kit (기기 OCR) ─────→ 글자 ──→ draft-parse
[캡처·웹]  끌어다 놓기/붙여넣기 → tesseract.js (브라우저) ┘   ↓
                                              draft-collect + draft-match
                                                          ↓
                                            POST /entry-drafts  (읽어 낸 값 + 원문 한 줄)
                                                          ↓
                        서버 EntryDraft ──(변경 피드)──→ 기기 사본 entry_draft / 웹 화면
                                                          ↓
                                  사용자가 "거래로 적기" → 값이 채워진 거래 추가 팝업
                                                          ↓
                                        기존 거래 생성 경로 → JournalEntry 생성
                                                          ↓
                                    PATCH /entry-drafts/:id (registered + 그 거래 id)
```

반복은 글자를 읽는 걸음이 없다. 사람이 적어 둔 값이 그대로 후보가 된다.

```
[반복]  사람이 적어 둔 RecurringRule (주기 + 담을 값)
              ↓  GET /recurring-rules   (lastMadeOn: 후보에서 센 "여기까지 만들었다")
        기기: dueOccurrences(규칙, 오늘) → 아직 만들지 않은 날들
              ↓  recurringDraftItems → draftPort().add → POST /entry-drafts
        서버: 열쇠·날짜·구간 검사 → EntryDraft (dedupeKey='r:<규칙>:<날짜>')
              ↓
        위의 캡처·알림 후보와 **같은 목록, 같은 등록 경로**
```

세 길(알림·캡처·반복)이 모두 **기기에서 값을 만들고 같은 창구로 담는다.** 창구
(`draftPort().add`)가 웹에서는 서버로만 올리고, 앱에서는 올린 뒤 받은 행을 사본에도
넣는다 -- 화면이 읽는 자리가 사본이라, 그 걸음이 없으면 "담았습니다"라는 말과 빈
목록이 함께 보인다.

**사진과 알림 원문은 서버로 가지 않는다.** 서버에 담기는 것은 읽어 낸 값과 근거가 된
원문 한 줄이고, 사진 자체는 기기(또는 브라우저 탭)를 떠나지 않는다. 해석을 전부
쓰는 쪽에서 하기로 한 결정(사용자 선택: 온디바이스 + 규칙 파서)이 그 성질을 만든다.

글자를 **얻는** 방법만 플랫폼이 정하고, 그 뒤(문구 → 값 → 맞춤 → 중복 열쇠)는
`draft-collect` 한 곳이다. 두 곳에 두면 같은 캡처가 웹과 앱에서 다른 후보가 된다.

---

## 3. 직접 판단한 것과 그 이유

### 3.1 후보를 명령 재생(아웃박스)에 태우지 않았다

거래·설정 엔티티는 오프라인 명령을 서버가 **도메인 서비스로 재생**한다(`mutations.ts`).
후보는 그 길을 쓰지 않는다. 후보에는 지켜야 할 불변식이 없고(합계 0, 결제수단 필수 같은
것), 어긋나면 다시 읽어 담으면 되는 값이다. 대신 두 가지를 두었다.

- **담기**는 서버로 곧바로 올린다. 실패하면 네이티브 버퍼에 그대로 남아 다음에 다시
  올라간다 — 버퍼가 곧 오프라인 큐다.
- **처리**(등록 표시·무시·지우기)는 사본에 먼저 적고 `draft_op` 한 줄을 남긴다. 동기화가
  **받기 전에** 그 큐를 비운다(`sync-engine` 의 `flushDrafts`). 순서를 뒤집으면 서버의
  `pending` 이 사본의 `registered` 를 덮어써서 같은 거래를 두 번 적게 된다. 그 자리를
  `inbox-mirror-smoke` 가 검사한다.

### 3.2 후보는 변경 피드에 실었다

"서버 + 기기 사본(동기화)"을 고른 결정에 맞추어, `EntryDraft` 에 다른 표와 같은
`updatedVersion` 트리거를 걸고 `/sync/pull` 에 실었다. 그래서 한 기기가 알림으로 담은
후보를 웹에서 정리하면 그 결과가 다시 기기 사본에 온다. 따로 조회하게 두면 화면마다
"언제 다시 물을지"를 정하게 되어 같은 보관함이 자리마다 다르게 보인다.

### 3.3 필드별 병합(fieldHlc)을 두지 않았다

후보는 여러 사람이 같은 칸을 동시에 고칠 물건이 아니다(대개 담은 기기에서 한 번 손보고
끝난다). 늦게 쓴 것이 이긴다.

### 3.4 결제수단은 네 가지 단서로 찾는다 (2026-09-09)

순서대로 보고, 걸리는 것이 하나뿐일 때만 채운다.

1. **카드 번호 끝 네 자리.** 가장 확실하다.
2. **등록해 둔 카드 이름이 글에 그대로 있는가.** 카드사 앱의 이용내역은 거래마다
   카드 줄을 따로 적는다("nori 체크카드(2395)"). 그 이름을 이 가계부에 그대로
   등록해 두었다면 어느 카드인지 의심할 자리가 없다.
3. **카드 이름 안의 카드사 이름.** "신한 체크"로 등록해 둔 카드가 신한카드 알림과 맞는다.
4. **통장 이름 안의 은행 이름.** 입금·출금은 카드가 아니라 통장에서 난다.

카드 줄을 다루는 데 걸린 자리 셋.

- **카드 줄은 거래 줄 아래에 온다.** 한 거래가 세 줄이다(가맹점·금액 / 시각·일시불 /
  카드). 그래서 방금 만든 후보에 붙인다.
- **앞뒤 줄의 카드 단서를 쓰지 않는다.** 가맹점을 찾기 위해 앞뒤 줄을 함께 보는데,
  그대로 두면 앞 거래의 카드가 이 거래에 붙는다. 실제 캡처에서 "버스" 거래에 앞
  거래("씨유")의 카드가 붙었다.
- **이름을 잃은 카드 줄에서도 끝자리는 건진다.** OCR 이 "nori 체크카드(2395)" 를
  "nori M=7t=(RF(2395)" 로 읽는다. 이름으로는 못 찾지만 끝자리로 찾을 수 있다.

실제 캡처(거래 21건)에서 **18건이 카드를 찾았다.** 못 찾은 3건은 카드 줄이
"1140705166카드(6039)" 로 읽혀 이름이 사라진 것들이다 -- 그 카드에 번호를 등록해
두면 끝자리로 찾는다.

### 3.5 값을 억지로 채우지 않는다

읽지 못한 칸은 비운다. 틀린 값이 채워져 있으면 사람은 그것을 검사하지 않고 저장한다.
같은 이유로 두 가지를 명시적으로 막았다.

- **카드사 알림은 통장으로 내려가지 않는다.** "국민카드 승인"에 맞는 카드가 없을 때
  "국민은행 통장"을 골라 주면 카드 사용액과 통장 잔액이 함께 틀린다.
- **같은 카드사 카드가 둘 이상이면 손대지 않는다.** 어느 것인지 모르는 채로 하나를
  고르면 다른 카드의 사용액이 조용히 틀린다.

분류만 예외로 짐작한다 — 같은 가맹점의 **가장 최근** 거래가 쓴 분류를 이어 쓴다(가장
많이 쓴 것을 세지 않는다. 분류를 바꾼 사람의 뜻이 옛 기록의 수에 지면 안 된다).

### 3.6 "누적 사용액"을 금액으로 읽지 않는다

카드 알림은 승인 금액과 누적 사용액을 함께 적는다. 누적을 집어 오면 커피 한 잔이 한 달
사용액으로 들어온다. 숫자 **바로 앞 열 글자**에 누적·잔액·한도 같은 낱말이 있으면 그
숫자는 버린다(줄 전체를 보면 한 줄로 오는 문자 알림에서 승인액까지 함께 버린다).

### 3.7 캡처에서는 단위 없는 숫자도 금액으로 본다

기기 OCR 은 "원"을 자주 잃는다. 에뮬레이터에서 실제로 `12,000원` 이 `12,000`,
`45,300l`, `4,800%` 로 왔다. 그래서 **캡처에서만** 자릿점이 있는 숫자를 금액으로 본다
(`12,000`은 금액이지만 `1234`는 카드 끝자리, `2026`은 연도다). 알림에는 그 규칙을 쓰지
않는다 — 문구가 깨끗하고, 거기서 단위 없는 숫자까지 읽으면 시각과 끝자리가 금액이 된다.

### 3.8 취소·환불은 수입으로 읽는다

돈이 돌아온 것은 맞고, 파서는 어느 거래를 되돌린 것인지 알 수 없다. 원래 지출을 찾아
지우는 일은 사람이 보관함에서 판단한다.

### 3.9 무시(dismissed)와 지우기를 갈랐다

무시한 후보를 지워 버리면 같은 알림이 다시 도착했을 때 유일 열쇠가 비어 있어 되살아난다.
무시는 표시를 남겨 그 자리에서 조용히 걸리게 하고, 지우기는 그 표시까지 없앤다.

### 3.10 등록 표시는 거래를 만든 **뒤에** 남긴다

순서를 뒤집으면 저장이 실패했을 때 아무도 그 후보를 다시 보지 못한다. 반대 순서에서
생기는 문제(거래는 남고 후보는 대기)는 목록에서 눈으로 걸러진다.

### 3.11 웹의 캡처는 브라우저에서 읽는다 (tesseract.js)

웹에 ML Kit 은 없다. 그렇다고 사진을 서버로 보내면 이 기능의 성질(사진이 기기를 떠나지
않는다)이 웹에서만 깨진다. 그래서 브라우저에서 도는 tesseract.js(WASM)를 쓴다.

- 한국어 모델과 WASM 은 **처음 한 번** tesseract.js 기본 경로(CDN)에서 받아 브라우저에
  캐시한다. 첫 인식이 느린 까닭이고, 화면에 그렇게 적어 두었다.
- 일꾼(worker)은 탭이 살아 있는 동안 하나만 둔다. 사진마다 세우면 두 번째 사진도
  처음처럼 느리다. 화면을 떠날 때 놓아 준다(WASM 메모리가 수십 MB다).
- 여러 장을 놓으면 한 장씩 읽는다. 동시에 읽으면 일꾼 하나를 서로 기다리고, 진행
  표시도 어느 사진의 것인지 알 수 없다.
- **긴 사진은 잘라서 읽는다.** 화면 캡처는 세로로 아주 길다(실제 카드 앱 이용내역이
  1440x12567 이었다). 통째로 넘기면 tesseract 가 절반쯤만 읽는다. 2000px 씩,
  160px 겹쳐 자른다.
- **겹친 자리는 좌표로 걷어낸다.** 조각의 글자를 이어 붙이지 않고 사진 전체의 좌표로
  모은 다음, 같은 자리를 두 번 읽은 것은 버린다. 글자로 비교하면 안 된다 -- 같은 줄을
  두 조각이 다르게 읽어서("59.800워" / "59,800원") 같은 줄인지 알 수 없다.
- **정확도는 ML Kit 보다 낮다.** 앱이 "카카오T 택시"로 읽은 것을 웹은 "카카오 택시"로,
  "CU 성수점"을 "04 성수점"으로 읽었다(2026-09-08). 금액과 날짜는 둘 다 맞았다.

알림 등록은 여전히 앱만 한다. 그 탭에는 그 사실을 적어 두었다 — 감추면 이 화면만 본
사람은 왜 아무것도 담기지 않는지 알 수 없다.

### 3.12 올리는 길을 셋 두었다

끌어다 놓기·눌러 고르기·붙여넣기다. 화면 캡처는 대개 방금 찍은 것이라 파일 탐색기를
여는 것보다 끌어다 놓는 편이 짧고, 윈도의 캡처 도구와 맥의 ⌘⇧4 는 클립보드에 그림을
남기므로 붙여넣기가 가장 짧다. 파일 고르기는 그 둘이 안 되는 자리(모바일 브라우저)를
위해 남긴다. 사진이 아닌 파일을 놓으면 조용히 무시하지 않고 그렇다고 말한다.

### 3.13 실제 카드 앱 캡처가 알려 준 것 (2026-09-09)

KB국민카드 이용내역 캡처(1440x12567, 거래 21건)를 웹에서 올려 보고 고친 자리들이다.
전부 `draft-parse-smoke` 에 그 문구 그대로 들어 있다.

| 무엇이 틀렸나 | 어떻게 고쳤나 |
| --- | --- |
| "국내 이용금액 579,190원"이 거래로 들어왔다 | 금액 앞 낱말에 `이용금액·이용액·사용액·청구금액`을 더했다 |
| "2'7,000원"을 7,000원으로 읽었다 | 숫자 사이의 따옴표를 자릿점으로 본다 |
| "2.900원"을 900원으로 읽었다 | 세 자리 뒤의 점을 자릿점으로 본다 |
| "표처럼 벌어진 칸"에서 낱말 규칙이 닿지 않았다 | 줄 안의 여백을 하나로 줄인다 |
| "08월2'/일"을 8월 2일로 읽었다 | **읽다 만 날짜 머리는 쓰지 않고** 앞 날짜를 이어 쓴다 |
| "해외 이용금액 USD 1.20"이 1월 20일이 되었다 | 합계·통화가 적힌 줄은 날짜 머리로 보지 않는다 |
| "지하철 1 건"이 가맹점 이름이 되었다 | `N건` 표기를 이름에서 뗀다 |
| "13,0008", "59,800워"의 꼬리가 이름에 남았다 | 금액 뒤 한 글자까지 뗀다 |
| OCR 이 만든 "¥"가 원화를 엔화로 바꿨다 | **캡처에서는 통화 기호를 믿지 않는다.** 글자 코드(USD·JPY)만 본다 |
| 같은 줄이 두 조각에서 두 번 읽혀 후보가 늘었다 | 좌표로 겹침을 걷어낸다 |

고친 뒤: 사진에 있는 거래 21건이 후보 21건이 되었고 금액은 전부 맞았다. 남은 것은
가맹점 이름 두 개(OCR 이 글자를 잃은 것)와, 날짜 머리를 읽지 못한 줄들이 앞 날짜를
이어 쓴 것이다.

**"총 54건"에 속지 말 것.** 카드 앱은 조회 기간의 건수를 머리에 적지만 캡처에는 화면에
담긴 것만 있다. 그 캡처에는 21건이 담겨 있었다.

### 3.14 앱은 보관함을 열 때 조용히 한 번 담는다

앱이 꺼져 있는 동안 온 알림이 버퍼에 쌓여 있다. 이 걸음이 없으면 보관함이 비어 보인다.
담을 것이 없으면 아무 말도 하지 않는다.

### 3.15 사본에 유일 색인을 걸지 않는다 (2026-09-09에 겪은 것)

사본의 `entry_draft` 에 (프로젝트, dedupeKey) 유일 색인을 걸어 두었더니 동기화가
멈췄다 -- `UNIQUE constraint failed: entry_draft.projectId, entry_draft.dedupeKey`.

후보를 지우고 같은 캡처를 다시 올리면 **같은 열쇠에 다른 id** 인 행이 생긴다. 델타는
새 행을 먼저 보내고 자리표(옛 행 삭제)를 뒤에 보내므로, 그 사이에는 두 행이 같은
열쇠를 갖는 것이 정상이다. **사본이 서버가 받아들인 것을 거절해서는 안 된다.**
유일함은 서버가 지키고 사본은 그림자다. 색인을 보통 색인으로 바꾸고 사본 스키마를
12 로 올렸다. `inbox-mirror-smoke` 가 그 순서를 검사한다.

### 3.16 반복도 거래를 만들지 않는다 (2026-09-09)

정해진 날이 되면 **후보**가 만들어진다. 전표는 사람이 "거래로 적기"를 눌러야 생긴다.

자동 기장을 하지 않은 까닭은 반복이 틀리는 방식 때문이다. 월세가 오르고, 구독을
끊고, 카드를 바꾼다. 자동으로 적히면 그 어긋남이 합계와 예산에 먼저 나타나고 사람은
나중에 발견한다. 후보로 두면 어긋남이 "적기 전"에 보인다.

폼 아래에도 그 사실을 적어 두었다(`inbox.ruleNote`). 적어 두지 않으면 "자동으로
가계부에 적히는 것"으로 읽고, 그 오해는 합계가 틀렸다는 신고로 돌아온다.

### 3.17 반복의 날짜는 글자로 저장한다

`RecurringRule.startDate`·`endDate` 는 `DateTime` 이 아니라 **TEXT "YYYY-MM-DD"** 다.

반복이 다루는 것은 시각이 아니라 **달력의 날**이다. `DateTime` 으로 두면 저장·읽기
어느 쪽에서든 UTC 로 바뀌며, 한국 시간 새벽에 만든 "9월 9일"이 "9월 8일"이 된다.
글자로 두면 그 변환이 아예 없다.

만들어지는 후보의 `occurredAt` 은 시각이 필요하므로, 그 자리에서 프로젝트 타임존의
`시각`(없으면 정오)을 붙여 UTC 로 옮긴다(`zonedFormValueToUtc`).

### 3.18 회차는 **기기가** 만든다. 서버는 구간만 본다 (2026-09-09에 옮겼다)

처음에는 서버가 만들었다(`recurring.service` 의 `run`, `scripts/run-recurring.ts` cron).
그것을 기기로 옮겼다. 까닭:

- **후보를 담는 길이 하나여야 한다.** 알림과 캡처는 이미 기기가 만들어
  `POST /entry-drafts` 로 올린다. 반복만 서버가 만들면 같은 일을 두 곳에서 하게 되고,
  두 곳이 어긋났을 때 어느 쪽이 맞는지 판정할 근거가 없다.
- 서버의 정기 작업(cron)이 사라진다. 걸어 두지 않으면 도는 곳이 없던 자리다.

지금의 길:

1. 화면이 반복 목록을 읽는다 (`GET /recurring-rules`, `lastMadeOn` 이 함께 온다).
2. 기기가 밀린 날을 셈해 후보 모양으로 만든다 (`recurringDraftItems`).
3. 창구로 담는다 (`draftPort().add` → `POST /entry-drafts`).

**서버가 하는 일은 구간 검사와 겹침 막기다.**

- 열쇠가 `r:<규칙 id>:<날짜>` 모양이고 그 날짜가 후보의 `occurredAt` 과 같은가
- 그 규칙이 이 가계부의 것인가
- 그 날이 **오늘부터 31일 전까지** 안인가 (앞당겨진 시계가 다음 달 월세를 지금
  넣는 것을 막는다)

어긋나면 요청 전체를 거절한다(`DRAFT_RECURRING_INVALID`). 반복은 사람이 적어 둔 값이라
"읽다 실패할" 자리가 없고, 어긋났다면 그것은 시계나 코드의 문제다. 자정 무렵 기기가
몇 분 빨라 거절되어도 잃는 것은 없다 -- 만든 것이 없으니 다음에 열 때 담긴다.

겹침은 전과 같이 `(프로젝트, dedupeKey)` 유일 제약이 막는다. 그래서 여러 기기가 같은
반복을 동시에 올려도, 같은 사람이 하루에 열 번 열어도 후보는 한 번만 만들어진다.

### 3.18.1 "어디까지 만들었는가"를 저장하지 않는다

`lastRunOn` 칸을 두었다가 없앴다. 대신 목록을 줄 때 **후보에서 센다** -- 그 규칙의
후보 중 열쇠(`r:<규칙>:<날짜>`)의 최댓값이 곧 마지막으로 만든 날이고, 그것을
`lastMadeOn` 으로 실어 보낸다(규칙 여럿을 한 번의 groupBy 로 센다).

표를 따로 들고 있으면 **올리다 끊긴 회차가 "만들었다"로 남는다.** 세 건을 올리다 두
건만 들어갔는데 표가 마지막 날로 옮겨 가면, 빠진 하루는 아무도 다시 만들지 않는다.
후보 자신을 세면 그런 자리가 없다.

값으로 하나 더 있다. 사람이 후보를 **지우면**(휴지통) 그 회차는 다시 만들어진다. 무시
(X)는 행이 남으므로 다시 만들어지지 않는다. 알림 후보의 지우기·무시와 같은 뜻이라
설명할 것이 늘지 않는다.

### 3.19 밀린 회차는 31일·31건까지만 따라잡는다

`RECURRING_CATCH_UP_DAYS = 31`, `RECURRING_MAX_PER_RUN = 31`.

한 달을 앱을 열지 않은 사람의 보관함에 매일치 후보가 서른 건 쌓이는 것까지는 뜻이
있다. 반년을 열지 않은 사람에게 이백 건을 만들어 주는 것은 보관함을 못 쓰게 만드는
일이다. 넘칠 때 **오래된 쪽을 버리고 최근 것을 남긴다** -- 어제 나간 돈이 3월에 나간
돈보다 적을 확률이 높다.

### 3.20 반복 규칙은 기기 사본에 두지 않았다

목록을 서버에서 곧바로 읽고 쓴다(`apiClient`). 후보와 달리 사본에 두지 않은 까닭:

- 반복은 한 번 만들어 두고 몇 달을 그대로 쓰는 **설정**이다. 비행기에서 고칠 일이
  드물다.
- 회차를 담는 것은 서버가 받아야 하는 일이라(겹침을 유일 제약이 막는다) 오프라인에서
  만든 규칙은 어차피 연결이 돌아온 뒤에야 후보를 낸다.

그렇게 만들어진 **후보는** 여느 후보와 같이 사본으로 내려온다. 그래서 연결이 없어도
보관함의 목록과 "거래로 적기"는 그대로 된다.

### 3.21 그 달에 없는 날은 말일에 만든다

"매월 31일"은 2월에 28일(윤년 29일), 4월에 30일이 된다(`onDayOfMonth`). 건너뛰면
"매월 나가는 돈"이 어떤 달에만 없다. 폼에서도 29일 이상을 고르면 그 사실을 적어
준다(`inbox.ruleEndOfMonth`).

주(weekly)는 넣지 않았다. `일단위 · 7일마다`가 시작일의 요일을 지키므로 같은 일을
한다. 갈래를 하나 더 두면 폼과 셈과 문구가 모두 늘어난다.

### 3.22 앱의 시각은 글자로 받고 그 자리에서 본다

웹은 `<input type="time">` 이 있어 "25:99"가 들어올 수 없지만, 앱에는 시각 입력이
없다. 글자로 받고 `^([01]\d|2[0-3]):[0-5]\d$` 로 본다. 틀리면 저장하지 않고 그렇게
적으라고 말해 준다(`inbox.ruleTimeInvalid`) -- 조용히 버리면 사람은 적어 둔 시각이
왜 사라졌는지 알 수 없다.

### 3.23 알약 줄은 고른 것을 보이는 자리로 민다

앱의 "며칟날"은 알약 서른한 개가 옆으로 넘어가는 줄이다. 그대로 두면 25일을 골라 둔
폼이 늘 "1일"부터 보여, 아무것도 고르지 않은 것처럼 읽힌다. `Chips` 에
`revealSelected` 를 두어 고른 알약이 보이는 자리까지 민다.

`Field`·`Chips` 는 거래 추가 팝업 안에 있던 것을 `components/FormFields.tsx` 로 뺐다.
두 벌로 두면 한쪽만 고쳐져 같은 폼이 화면마다 다르게 보인다.

### 3.24 캡처의 시각도 읽어 후보에 담는다 (2026-09-09)

카드사 앱의 이용내역은 한 거래를 세 줄로 적는다 — 가맹점·금액 / **시각**·일시불 /
카드 이름. 날짜는 목록 머리에서 이어 쓰는데 그 머리에는 시각이 없어서, 모든 후보가
그날 정오(`atLocal` 의 기본값)로 담겼다. 폼은 후보의 `occurredAt` 에서 날짜와 시각을
함께 가져오므로(`entryFormFromDraft`), 사람이 거래마다 시각을 다시 적어야 했다.

- 시각은 **그 거래 줄**, 없으면 **뒤 줄**에서 읽는다(`timeOf`). 그 줄에 금액이 있으면
  다음 거래의 줄이라 보지 않는다 — 카드 줄을 뒤 줄에서 받지 않는 것과 같은 이유고,
  그러지 않으면 한 칸 밀린 시각이 붙는다.
- 읽은 시·분은 이어 쓰던 날짜에 얹는다(`withTime`). 로컬 벽시계로 얹으므로 날짜는
  그대로 남는다.
- **범위를 본다.** "59:800" 처럼 OCR 이 만든 자리를 그대로 쓰면 `Date` 가 다음
  시간·다음 날로 넘겨 시각뿐 아니라 날짜까지 밀린다.
- **날짜를 못 읽었으면 시각도 쓰지 않는다.** 오늘을 채워 넣으면 그 후보가 "날짜를 아는
  후보"로 보여, 사람이 그 칸을 검사하지 않고 저장한다(3.5 와 같은 이유다). 비워 두면
  폼이 오늘로 열리는 것은 같지만 보관함 줄에 날짜가 없어 손볼 자리인 것이 보인다.

알림은 예전부터 문구에서 시각을 읽었다(`dateOf`). 그 규칙 하나를 캡처가 함께 쓰게 된
것이고, 웹과 앱이 같은 함수를 지난다.

### 3.25 날짜 머리를 잃으면 그 아래 거래가 딴 날로 간다 (2026-09-09)

실제 캡처(1440x12567, 54건)에서 9월 3일 "어향"이 **9월 5일**로 담겼다. 좇아가 보니
날짜 머리 세 개가 한꺼번에 무너져 있었다.

| 사진의 머리 | OCR 이 읽은 것 | 그래서 |
| --- | --- | --- |
| 09월04일 | `No<un4o!` | 아래 4건이 09-05 로 |
| 09월03일 | `09803` | "어향"이 09-05 로 |
| 09월01일 | `098012` | 아래 1건이 09-05 로 |
| 09월07일 | `09월0'/일` | 아래 1건이 08-31 로 |

머리를 못 읽으면 앞 머리의 날짜를 이어 쓰는데(3.13), 목록은 날짜가 **내려가는** 순서라
그 값은 늘 실제보다 **새 날짜**다. 두 자리를 고쳤다.

- **`No<un4o!` 는 조각 경계에 걸린 줄이었다.** 그 줄(y=3803)은 앞 조각(1840~3840)의 끝에
  반쪽만 담겼고, 다음 조각(3680~5680)에는 온전히 담겨 "09월04일"로 제대로 읽혔다. 겹친
  자리를 "먼저 읽은 것"으로 고르던 규칙이 **무너진 쪽을 남기고 제대로 읽은 쪽을 버렸다.**
  이제 조각 가장자리에서 **먼 것**을 남긴다(`edge`). 웹과 앱 양쪽을 같이 고쳤다.
- **`09803`·`098012` 는 `월`·`일` 글자를 숫자로 읽은 것이다.** 구분자가 없어 날짜로
  읽히지 않았다. 앞 두 자리를 달, 한 자리 건너 두 자리를 날로 보아 되살린다
  (`repairedDateHeader` → "09월03일"). 되살린 머리가 **앞서 본 날짜보다 새것이면
  버린다** -- 내려가는 순서가 아니라면 그 숫자는 날짜가 아니었다는 뜻이고, 그대로
  쓰면 그 아래 거래 전체가 엉뚱한 날로 간다.

`09월0'/일` 은 되살리지 않는다. 잃은 것이 구분자가 아니라 **날짜의 숫자**(7)라서,
남은 것으로는 무엇이었는지 알 수 없다. 그 자리는 앞 날짜를 이어 쓴다.

고친 뒤 같은 사진에서 25건 중 22건이 사진에 적힌 날짜로 담겼다(앞의 `09월0'/일` 한 건과,
머리가 사진 위쪽으로 잘려 나간 두 건은 남는다). 되풀이해 보는 방법은 5장에 적었다.

---

## 4. 안드로이드 쪽 사정

### 4.1 알림 접근 권한

`BIND_NOTIFICATION_LISTENER_SERVICE` 는 런타임 요청으로 받을 수 없다. 사용자가
**설정 > 알림 접근**에서 이 앱을 직접 켜야 하고, 화면은 "확인 + 설정 열기" 두 걸음으로
안내한다. 무엇을 읽고 무엇을 보내지 않는지도 그 자리에 적었다.

### 4.2 안드로이드 15 는 일부 알림을 가린다 (중요)

OTP 로 분류된 알림은 제3자 리스너에게 본문 대신 `Sensitive notification content hidden`
이 온다(안드로이드 15 동작 변경). **카드 승인 문구에 끝 네 자리가 붙어 있으면 그
분류에 걸리는 것을 에뮬레이터에서 확인했다.** 가려진 문구에는 금액이 없어 후보가 되지
않고 조용히 버려진다.

그래서 두 탭이 서로를 메운다 — 알림이 가려지는 결제는 카드사 앱을 캡처해서 적는다.
이 한계는 앱이 우회할 수 있는 것이 아니다(면제는 동반 기기 관리자 같은 신뢰 앱뿐이다).

### 4.3 로컬 Expo 모듈

`packages/app/modules/inbox-native` 는 `npx create-expo-module --local` 이 만드는 모양을
손으로 적은 것이다. 안드로이드만 있고(`platforms: ["android"]`), 자동 링크는
`expo-modules-autolinking` 이 프로젝트의 `modules/` 를 훑어 찾는다
(`npx expo-modules-autolinking search --platform android` 로 확인할 수 있다).

주의할 것 둘.

- `android/build.gradle` 에 `defaultConfig { versionCode / versionName }` 이 **있어야
  한다.** 없으면 autolinking 이 `'android.defaultConfig.versionName' is not defined` 로
  설정 단계에서 멈춘다.
- 네이티브를 고치면 `npx expo run:android` 로 다시 빌드해야 한다. Metro 를 새로 고치는
  것만으로는 반영되지 않는다.

`AndroidManifest.xml` 은 모듈 쪽에 두었고 앱 매니페스트에 병합된다.

### 4.4 OCR 은 보이는 순서로 되돌린다

ML Kit 이 주는 블록 순서는 목록 화면에서 열을 따라 묶인다("가맹점 세 줄" 다음에 "금액
세 줄"). 그대로 쓰면 어느 금액이 어느 가게의 것인지 알 수 없다. 그래서 가운데 높이가
비슷한 조각을 한 줄로 묶고 줄 안에서는 왼쪽부터 잇는다(`layoutText`). 목록 캡처에서
가맹점과 금액이 같은 줄에 서고, 파서는 그 한 줄만 보면 된다.

### 4.5 앱도 긴 사진을 잘라 읽는다 (2026-09-09)

웹에 둔 것과 같은 일을 네이티브에도 두었다. 통째로 넘기면 인식기가 사진을 제 크기로
줄여 보므로 글자가 뭉개져 목록의 절반이 빠진다 — 같은 사진(1440x12567)에서 웹이 54건
중 23건만 읽은 것이 그 자리였다. `TILE_HEIGHT` 2000px, `TILE_OVERLAP` 160px 으로
웹(`capture-ocr.ts`)과 같은 값을 쓴다.

- 조각은 **필요한 자리만 떠서** 읽는다(`BitmapRegionDecoder`, `isShareable = false`).
  사진 한 장을 펼치면 1440x12567 이 72MB 라 그것만으로 앱이 죽는다.
- 겹친 자리는 웹과 같은 규칙으로 좌표에서 걷어낸다(가로로 절반 넘게 겹치면 하나만
  남긴다). 글자로 비교할 수 없어서다. 남길 쪽은 **조각 가장자리에서 먼 것**이다(3.25).
- **눕혀 찍힌 사진(EXIF 회전)은 자르지 않는다.** 자르는 일은 화소를 그대로 쓰므로
  엉뚱한 축으로 자르게 된다. 그때는 통째로 `InputImage.fromFilePath` 에 넘겨 ML Kit 이
  회전을 바로잡게 한다 — 자르는 것이 필요한 긴 화면 캡처에는 회전이 붙지 않는다.
- 조각으로 뜰 수 없는 사진, 크기를 읽지 못한 사진, 한 조각도 읽지 못한 사진은 모두
  통째로 읽는 길로 되돌린다.
- OCR 은 `AsyncFunction ... Coroutine` 으로 두고 그 안에서 `Dispatchers.Default` 로
  옮긴다. 기본 갈래는 모듈 하나짜리 큐(`expo.modules.AsyncFunctionQueue`)라, 거기서
  조각마다 기다리면 그동안 다른 네이티브 부름(사본 읽기 등)이 함께 멈춘다.

---

## 5. 검사

```bash
cd packages/core
node -r ../api/node_modules/ts-node/register/transpile-only scripts/draft-parse-smoke.ts
node -r ../api/node_modules/ts-node/register/transpile-only scripts/inbox-mirror-smoke.ts
node -r ../api/node_modules/ts-node/register/transpile-only scripts/recurring-smoke.ts
```

- **draft-parse-smoke** — 카드 승인·통장 입금·해외 승인·취소·지난해 결제·₩ 표기·
  OCR 이 단위를 잃은 캡처·**캡처의 시각(뒤 줄·같은 줄·범위를 벗어난 자리)**·금융이
  아닌 알림·중복 열쇠·자산 맞춤까지 50여 건.
- **inbox-mirror-smoke** — 델타 적용, 자리표, 오프라인 등록 표시, **큐가 남은 동안
  델타가 후보를 되살리지 않는가**, 큐를 비운 뒤에는 서버 값이 이기는가, 탭 고르기.
- **recurring-smoke** — 매일·며칠마다·매월·말일 맞춤(2월 28/29일)·매년·끝나는 날·
  오래 밀린 규칙의 잘라내기·잘못된 일정 걸러내기, 그리고 회차를 후보 모양으로 옮기는
  것(열쇠·정오·정해 둔 시각·꺼 둔 반복·상한)까지 30여 갈래.

캡처 파서를 고칠 때는 **사진에서 읽은 글자로 되풀이해 보는 것**이 가장 빠르다. 브라우저
없이 그 글자를 얻는 방법은 이렇다 (2026-09-09에 9월 3일 "어향" 문제를 이렇게 좇았다).

```bash
# 1. 사진을 2000px 씩 자른다 (sips 는 offsetY + 높이가 사진 높이와 **같으면** 거절한다).
for top in 0 1840 3680 5520 7360 9200 10560; do
  sips -c 2000 1440 --cropOffset $top 0 거래내역.jpeg --out tiles/t$top.png
done
# 2. tesseract.js 로 조각마다 읽고, 조각 안 좌표·조각 높이를 함께 남긴다 (웹과 같은 값).
# 3. layoutLines 와 같은 규칙으로 이어 붙여 글자를 만든다.
# 4. parseCaptureText 에 그 글자를 넣어 후보의 날짜·금액·카드를 표로 찍어 본다.
```

에뮬레이터에서 확인한 것 (2026-09-08, Android 15 / API 35).

1. `adb shell cmd notification post` 로 낸 알림 셋 중 금융 알림 둘만 버퍼에 담겼고
   (할인 쿠폰 알림은 걸러졌다) 보관함에 후보 2건이 생겼다.
2. 만들어 둔 카드 이용내역 캡처를 골라 후보 5건이 생겼다("누적 사용액" 줄은 빠졌다).
3. 후보의 "거래로 적기" → 값이 채워진 팝업 → 저장 → `JournalEntry` 가 실제로 생기고
   (다리 합계 ±12,000) 후보가 `registered` 로 바뀌며 목록에서 빠졌다.
4. 웹에서도 같은 후보 목록이 보이고, "거래로 적기"가 값이 채워진 팝업을 띄웠다.
5. **웹에 캡처를 끌어다 놓아** 후보 5건이 생겼다(브라우저 OCR). 붙여넣기(⌘V)도 같은
   결과였다. `next build` 에서 `/transactions/inbox` 는 8kB 이고, tesseract.js 는
   동적 import 라 첫 화면 번들에 들어가지 않는다.
6. **캡처에 적힌 카드 이름으로 카드가 세팅된다.** 실제 캡처의 거래 21건 중 18건이
   "nori 체크카드"·"ktMmobile 신용카드"를 찾았고, 후보 줄에 그 이름이 보인다(웹·앱 둘 다).
7. **웹에서 눌러 고르기**도 확인했다. 실제 카드 앱 캡처(1440x12567)를 파일 고르기
   창으로 올려 후보 21건이 생겼고(사진에 담긴 거래 수와 같다) 금액은 전부 맞았다.
   읽는 동안 진행 표시("읽는 중 9%")가 뜬다. 7 조각을 읽는 데 7초였다(모델은 이미
   받아 둔 상태).

반복은 웹과 앱에서 각각 확인했다 (2026-09-09).

8. **웹**에서 `반복 추가` → "월세 · 매월 15일 · ₩550,000" 을 저장했고 줄에 `다음
   2026-09-15` 가 붙었다.
9. **앱**에서 그 반복이 그대로 보이고, `반복 추가` 로 "Netflix · 매월 10일 ·
   ₩17,000" 을 저장했다. `켜 둠`을 눌러 끄면 `예정 없음`이 되고 흐려진다. 연필로
   열면 적어 둔 값이 그대로 채워지고, `반복 지우기`는 한 번 묻고 지운다.
10. 하루 단위 반복(시작일을 이틀 앞으로 둔 것)을 만들자 **그 자리에서 밀린 3회차가**
    후보로 생겼고(9/7·9/8·9/9), 같은 호출을 다시 해도 `{created:0, skipped:0}` 으로
    아무것도 늘지 않았다.
11. 서버에서 규칙과 후보를 지우자 앱의 사본에서도 사라졌다(자리표가 내려온다).

회차를 기기로 옮긴 뒤 다시 확인했다 (2026-09-09).

12. `POST /recurring-rules/run` 은 404 다(길이 없어졌다). 규칙만 만들어 두면
    `GET /recurring-rules` 의 `lastMadeOn` 은 null, `nextRunOn` 은 오늘이다.
13. **웹**에서 보관함을 열자 밀린 3회차가 담기고("반복에서 후보 3건을 만들었습니다")
    줄의 다음 예정일이 그 다음 날로 옮겨 갔다. 다시 열면 `created 0 / skipped 3` 이라
    아무것도 늘지 않았다.
14. **앱**에서도 같았고, 담긴 후보가 **그 자리에서** 목록에 보였다(사본에 함께 넣는다).
15. 구간 검사 네 갈래를 손으로 찔러 보았다 -- 미래 날짜·31일보다 오래된 날짜·날짜와
    맞지 않는 열쇠는 `DRAFT_RECURRING_INVALID`, 남의 규칙 id 는 `RECURRING_NOT_FOUND`.
16. 알림 후보도 그대로다. 에뮬레이터에 카드 승인 알림을 하나 넣고 "지금 확인하기"를
    누르자 후보 1건(₩3,200 파리바게뜨)이 담기고 곧바로 목록에 보였다 -- 창구를 거치게
    바꾼 길이 알림·캡처에서도 같이 돈다.

검사와 확인에 쓴 후보·거래·반복은 모두 지웠다. 사용자가 실제 캡처로 만든 후보 21건은
그대로 두었다.

---

## 6. 남은 것

- **알림 문구 규칙은 표본이 늘어야 한다.** 지금 규칙은 널리 쓰이는 서식을 겨냥한
  것이고, 실제 문구를 만나면 `draft-parse-smoke` 에 그 문구를 넣고 규칙을 고치는
  방식으로 늘린다. 규칙 이름(`parser`)이 후보에 함께 담기므로 어느 규칙이 틀렸는지
  되짚을 수 있다.
- **후보를 손질하는 화면이 없다.** 지금은 "거래로 적기"로 폼을 열어 고친다.
  `PATCH /entry-drafts/:id` 는 이미 값 손질을 받는다(창구의 `patch`).
- **아무도 열지 않으면 반복 후보가 만들어지지 않는다.** 회차를 기기가 만들기 때문이다
  (3.18). 열면 그 자리에서 31일까지 따라잡으므로 사람이 보는 화면은 늘 맞지만, "월세
  낼 날입니다" 같은 **서버 알림**을 붙이려면 서버가 그 날을 알아야 한다. 그때는 회차
  만들기를 서버에도 두게 될 텐데, 열쇠가 같으므로 두 곳이 겹쳐 돌아도 된다.
- **정리 작업.** `DELETE /entry-drafts/prune?days=30` 은 있지만 부르는 일정이 없다.
  자리표(`Tombstone`)를 줄이려면 `scripts/prune-tombstones.ts` 처럼 주기적으로 부를
  자리가 필요하다.
- **iOS.** 알림 접근에 해당하는 것이 없어 캡처 인식만 가능하다. 지금 모듈은 안드로이드
  전용으로 두었다.
- **웹 OCR 자료를 자체 호스팅할 수 있다.** 지금은 tesseract.js 기본 경로(CDN)에서
  받는다. 사내망처럼 밖으로 나갈 수 없는 자리라면 `public/` 에 모델과 WASM 을 두고
  `createWorker` 의 `langPath`·`corePath`·`workerPath` 를 가리키면 된다.
- **분류 짐작은 가맹점 이름이 정확히 같을 때만 걸린다.** "스타벅스 강남2호점"과
  "스타벅스코리아"는 다른 이름으로 본다. 느슨하게 견주면 엉뚱한 분류가 채워지는 쪽이
  늘어서 지금은 좁게 두었다 -- 표본이 쌓이면 앞부분 일치 같은 규칙을 재 볼 수 있다.
- 캡처 인식은 카드사·은행 앱의 목록 화면을 겨냥했다. 영수증 사진은 서식이 달라 금액
  하나만 걸리는 경우가 많다.
