/**
 * 태그에 고를 수 있는 색.
 *
 * 자유 입력을 두지 않는다. 색을 직접 적게 하면 목록에서 서로 구별되지 않는 비슷한
 * 색들이 쌓이고, 앱에는 색 고르는 기본 위젯이 없다 (카드 색과 같은 방식이다).
 *
 * 네 자리가 함께 쓴다 -- 웹·앱의 태그 설정 판, 그리고 거래를 적다가 그 자리에서
 * 태그를 만드는 칸. 한 벌로 두지 않으면 같은 태그가 화면마다 다른 색으로 만들어진다.
 */
export const TAG_COLORS = [
  '#ef4444',
  '#f97316',
  '#eab308',
  '#22c55e',
  '#06b6d4',
  '#3b82f6',
  '#8b5cf6',
  '#ec4899',
] as const;
