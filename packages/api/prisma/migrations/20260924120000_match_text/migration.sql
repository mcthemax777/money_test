-- 알림·캡처 글에서 통장·카드를 알아보는 말.
--
-- 보관함이 문구를 읽어 후보를 만들 때(@money/core 의 draft-match) 가장 먼저 보는
-- 단서다. 끝 네 자리도 카드 이름도 적히지 않는 알림이 있고("[Web발신] KB국민 승인"),
-- 같은 카드사 카드가 둘이면 기기는 어느 것인지 알 수 없다. 그 글에 늘 함께 오는
-- 말을 사람이 한 번 적어 두면 그 뒤로는 저절로 채워진다.
--
-- 한 줄에 하나씩 담는다. 여러 개를 따로 담는 표를 두지 않는 것은, 이 값이 목록에서
-- 검색되거나 다른 행과 이어지지 않고 그 수단 하나에만 붙어 있기 때문이다.
ALTER TABLE "Account" ADD COLUMN "matchText" TEXT;
ALTER TABLE "Card" ADD COLUMN "matchText" TEXT;
