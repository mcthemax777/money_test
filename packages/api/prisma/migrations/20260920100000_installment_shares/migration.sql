-- 사용자가 적어 둔 회차별 원금.
--
-- 지금까지 회차 금액은 총액을 개월수로 나누고 끝수를 첫 회차에 몰아준 값이었다. 그런데
-- 끝수를 어디에 붙이는지는 카드사마다 다르다 -- 1,000원 3개월이 334/333/333 일 수도
-- 334/334/332 일 수도 있어, 계산만으로는 명세서와 맞출 수 없는 자리가 남는다.
--
-- 그래서 사람이 적은 값을 그대로 담는다. null 이면 지금까지처럼 나눈다.
ALTER TABLE "InstallmentPlan" ADD COLUMN "principalShares" JSONB;
