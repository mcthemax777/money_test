# 실시간 동기화 충돌 점검 (2026-09-06)

## 진행 상태 (2026-09-07)

**A~L 을 모두 처리했다.** 각 항목 머리에 무엇을 했는지 적어 두었고, 아래 본문은 착수
당시의 근거를 그대로 남긴 기록이라 줄 번호가 지금 코드와 다를 수 있다.

| 항목 | 내용 | 상태 |
| --- | --- | --- |
| A | 같은 거래 동시 수정 시 잔액이 어긋남 | 완료 (자문 잠금) |
| B | 온라인 경로에 충돌 판정 없음 | 완료 (`baseHlc` → `ENTRY_MODIFIED`) |
| C | 재시도가 전표 시계만 봄 | 완료 (`settingTableOf` 로 대상별 시계) |
| D | 지워진 거래를 고친 명령을 되살릴 길이 없음 | 완료 (보류 칸의 "새 거래로 다시 적기") |
| E | 태그 자리표가 사본에 연결을 남김 | 완료 |
| F | 숨기기 선행조건이 트랜잭션 밖 | 완료 (같은 자문 잠금) |
| G | 같은 태그 동시 부착 시 500 | 완료 (`skipDuplicates`) |
| H | 분류 숨기기가 시계를 안 찍음 | 완료 (+ 재생도 선행조건을 거치게) |
| I | 권한이 사라진 기기가 조용히 멈춤 | 완료 (사본 버리기 + 알림 + viewer 쓰기 버튼 감춤) |
| J | 프로젝트가 지워지면 사본에 유령 | 완료 (I 와 같은 경로) |
| K | 하드 삭제가 전표 번호를 안 올림 | 완료 (`EntryTag` 트리거) |
| L | 배치 경로가 신호를 안 냄 | **하지 않음** (사용자 결정: 지금은 필요 없다) |

검사와 도구도 함께 두었다.

- `packages/api/scripts/entry-concurrency-smoke.ts` -- 요청을 겹쳐 던지는 유일한 스모크다.
  잠금을 빼고 돌리면 잔액 검사 다섯 건이 사라진 중간 버전의 금액만큼 어긋난다.
- `packages/api/scripts/check-balances.ts` -- **읽기만 하는** 점검. 잔액과 다리 합계를
  대조해 어긋난 계좌를 찾고, 고칠 SQL 을 적어 준다. A 는 앞으로 생길 것만 막으므로
  그전에 어긋난 값은 이것으로 찾아야 한다.

---

두 사람이 **같은 것을 동시에 고칠 때**와 **한쪽이 다른 쪽이 쓰는 것을 지울 때** 지금 코드가
어떻게 대처하는지 훑었다. 읽은 범위는 아래와 같다.

- 서버: `packages/api/src/modules/sync`, `modules/realtime`, `common/sync-notify.middleware.ts`,
  `modules/entries`, `modules/ledger`, 자산 서비스 다섯(구성원·통장·카드·분류·태그), `modules/budgets`
- 데이터베이스: `packages/api/prisma/migrations` 의 도장·자리표 트리거 전부
- 기기: `packages/core/src/data` (sync-engine, sync-events, local-store, mirror-\*), `packages/app/src/offline.ts`,
  `packages/web/src/components/LiveSync.tsx`

**전제 하나.** `PrismaService` 도 스키마도 격리 수준을 지정하지 않는다. 따라서 모든 `$transaction`
은 PostgreSQL 기본값인 READ COMMITTED 로 돈다. 아래 경합 항목(A·F·G)은 그 전제 위에서 코드를
읽어 도출했고, **실제로 두 요청을 동시에 쏘아 재현하지는 않았다.** 고치기 전에 재현 스크립트를
먼저 쓰는 편이 안전하다.

---

## 1. 이미 제대로 되어 있는 것

고칠 것을 찾는 문서지만, 다시 건드리다 깨뜨리지 않도록 지켜지고 있는 것부터 적어 둔다.

- **번호 발급과 순서.** `sync_next_version()` 이 `Project` 행을 UPDATE 로 잠그므로 한 프로젝트의
  쓰기가 직렬화되고, 번호 순서와 커밋 순서가 같다. 도장을 서비스가 아니라 트리거에 둔 것도 옳다.
- **pull 의 상한과 자르기.** 상한(`syncVersion`)을 맨 처음 읽고 모든 질의를 그 아래로 묶으며,
  넘칠 때는 버린 첫 행의 번호 앞에서 끊는다. 어떤 변경도 반쪽만 실리지 않는다.
- **자리표 보관 기간.** 지우기와 바닥 올리기가 한 트랜잭션이고, 기기는 바닥이 실제로 올랐을 때만
  사본을 버린다(`sync-engine.ts` 의 `floor > mirrorFloor`). 처음 받는 기기가 되돌리기를 반복하지 않는다.
- **재생의 멱등.** 행을 먼저 잡고(`MutationLog` 유일 제약) 재생한다. 조회 후 기록 방식의 이중
  적용이 구조적으로 막혀 있고, 죽은 잡기를 넘겨받는 길도 조건부 UPDATE 로 안전하다.
- **잔액의 증분 갱신.** `applyBalanceDeltas` 가 `{ increment }` 를 쓴다(ledger.service.ts:1111).
  덧셈 자체는 원자적이다. 문제는 **얼마를 더할지 정하는 읽기**에 있다(항목 A).
- **삭제는 언제나 이긴다.** 자리표에 시계를 견주지 않는다. 지운 거래가 되살아나 두 번 세어지는
  일이 없다.
- **진 편집을 버리지 않는다.** 오프라인 경로는 충돌·거절을 보류 칸에 남기고 사용자가 고른다
  (`OutboxScreen`). 조용히 사라지는 돈이 없다 — **다만 온라인 경로에는 이 장치가 없다(항목 B).**
- **인스턴스 너머의 신호.** 레디스 pub/sub 로 흘리고, 끊기면 자기 프로세스 안으로만 흘린다.
  신호가 유실되어도 다음 pull 이 커서로 따라잡으므로 정확성이 상하지 않는다.

---

## 2. 고쳐야 할 것

심각도 순서다. 각 항목에 증상·재현·근거·고칠 방향을 적었다.

### A. 같은 거래를 동시에 고치면 통장 잔액이 어긋난다 (심각) — 완료

> **고친 내용.** `LedgerService.lockLedger()` 를 두고 `replaceEntry`·`deleteEntry`·
> `restateForeignEntry` 의 트랜잭션 맨 앞에서 부른다. 되돌릴 다리를 읽기 전에 줄을 세운다.
>
> 아래 "고칠 방향"의 1·2 번(전표 행 잠금, 프로젝트 행 잠금) 대신 **자문 잠금**
> (`pg_advisory_xact_lock`)을 썼다. 프로젝트 행을 잠그면 교착이 생긴다 -- 도장 트리거가
> 쓰기마다 그 행을 UPDATE 하므로 보통의 쓰기는 "제 행 → 프로젝트 행" 순서인데, 여기서
> 프로젝트 행을 먼저 잡으면 순서가 뒤집힌다. 이름은 잔액 맞추기(`setBalanceTo`)가 쓰던
> 것과 **하나로 합쳤다**. 그쪽도 "그 계좌의 나머지 거래 합"을 읽어 역산하므로 전표 수정과
> 줄을 서야 했는데, 지금까지는 서지 않았다.
>
> 3 번(복구 스크립트)은 하지 않았다. 이미 어긋난 데이터가 있다면 따로 다뤄야 한다.

**증상.** 두 사람이 같은 거래를 거의 동시에 저장하면, 전표와 다리는 나중 저장의 값으로 남지만
계좌 잔액은 **사라진 중간 버전의 금액만큼 어긋난 채** 남는다. 어떤 오류도 나지 않는다.

**재현(추정).** 원래 10,000원인 지출을 A는 20,000원으로, B는 30,000원으로 동시에 저장한다.

| 시점 | T1 (A) | T2 (B) |
| --- | --- | --- |
| 1 | 옛 다리 10,000 을 읽는다 | 옛 다리 10,000 을 읽는다 (T1 미커밋이라 같은 값) |
| 2 | 계좌 -10,000 (여기서 프로젝트 행 잠금을 잡는다) | 대기 |
| 3 | 다리 교체, 계좌 +20,000, 커밋 | |
| 4 | | 계좌 **-10,000 다시** (1번에서 읽은 옛 값) |
| 5 | | 다리 교체, 계좌 +30,000 |

전표는 30,000 인데 잔액은 20,000 만큼 움직여야 할 자리에서 30,000 움직인다.

**근거.** `ledger.service.ts:288-307` — 옛 전표를 트랜잭션 **안에서** 읽지만 잠그지 않는다.
첫 잠금은 그다음 `account.update` 가 트리거를 통해 잡는 프로젝트 행 잠금이고, 그때는 이미 옛
값을 손에 들고 있다. 주석은 "밖에서 읽으면 안 된다"까지만 짚었고, 안에서 읽어도 READ COMMITTED
에서는 스냅숏이 문장마다 새로 잡힌다는 점이 빠져 있다.

**고칠 방향.** 셋 중 하나다.

1. 트랜잭션 첫 문장에서 대상 전표를 `SELECT ... FOR UPDATE` 로 잠근 뒤 읽는다. 가장 좁은 수술이다.
2. 트랜잭션을 열자마자 `sync_next_version` 과 같은 프로젝트 행 잠금을 먼저 잡는다. 프로젝트 단위
   직렬화라 뜻은 확실하지만 처리량이 준다.
3. 잔액을 캐시로 보고 다리 합계에서 다시 계산하는 복구 스크립트를 함께 둔다. 1·2 를 해도 이미
   어긋난 데이터는 남으므로 이것은 어느 쪽이든 필요하다.

동시 삭제·동시 수정이 섞인 경우(수정 중 다른 쪽이 삭제)는 `journalEntry.update` 가 P2025 로
실패해 통째로 되돌아간다. 그쪽은 안전하다. **수정과 수정이 겹치는 경우만 위험하다.**

### B. 온라인 경로에는 충돌이라는 개념이 아예 없다 (심각) — 완료

> **고친 내용.** 목록 한 줄(`EntryListItem`)에 `updatedHlc` 를 싣고, 수정 요청이 그것을
> `baseHlc` 로 되돌려 준다. 원장이 **잠근 뒤** 지금 값과 견주어 다르면 409
> `ENTRY_MODIFIED` 로 거절한다. 견주는 자리가 잠금 안이어야 검사가 검사 노릇을 한다.
>
> 화면 셋을 함께 고쳤다. 웹 `EntryEditor` 와 앱이 쓰는 `useEntryForm` 이 폼을 열 때 본
> 시계를 들고 있다가 보내고, 오류 문구는 서버가 붙인 코드로 고른다(`messageOf`) -- 전에는
> "수정에 실패했습니다" 하나로 덮여 다시 눌러도 소용없다는 것을 알 수 없었다.
>
> **`baseHlc` 를 보내지 않으면 검사하지 않는다.** 옛 화면과, 시계가 없던 시절의 전표를
> 위한 자리다. 삭제에는 붙이지 않았다 -- 툼스톤이 언제나 이기는 규칙 그대로다 (D5).
>
> 재생 경로의 규칙은 건드리지 않았다. 그쪽은 **늦은 시계가 이기고** 진 편집을 보류 칸에
> 남긴다. 오프라인 명령에는 사람이 답할 자리가 없어 자동 병합이 필요하고, 지금 누른
> 사람에게는 최신 값을 보여 주고 다시 고르게 하는 편이 낫다.

**증상.** 웹에서 두 사람이 같은 거래를 열어 두고 각자 고치면, 늦게 저장한 쪽이 이기고 진 쪽은
**자기 편집이 사라진 것을 모른다.** 설계 문서 D6("돈은 말없이 사라지면 안 된다")이 오프라인
경로에만 적용되어 있다.

**근거.**
- `entries.service.ts:87` `updateEntry` 는 `updatedHlc` 도 `updatedVersion` 도 보지 않는다.
  같은 파일에서 `changeTags` 는 시계를 견주는데(238행) 정작 전표 교체는 견주지 않는다.
- 재생 경로만 `mutation-replay.service.ts:442` 에서 `isAfterHlc` 로 막고 충돌로 돌려준다.
- 태그도 같이 사라진다. `ledger.service.ts:1051` `saveTags` 는 연결을 통째로 지우고 다시 만든다.
  A 가 폼을 연 뒤 B 가 태그를 붙였다면, A 의 저장이 그 태그를 지운다.

**고칠 방향.** 수정 요청에 기기가 본 시계(또는 `updatedVersion`)를 실어 보내고, 서버가 다르면
409 로 돌려준다. 화면은 "다른 사람이 먼저 고쳤습니다. 다시 읽을까요, 내 값으로 덮을까요"를
묻는다. 재생 경로가 이미 같은 판정을 하고 있으므로 **판정 함수를 공유**하면 규칙이 하나로 남는다.
항목 A 의 잠금과 함께 고치는 것이 자연스럽다 — 잠근 뒤 읽으면 그 자리에서 시계를 견줄 수 있다.

### C. 충돌을 재시도해도 자산·분류·태그·예산은 다시 진다 (중간) — 완료

> **고친 내용.** `LocalStore.observedClock()` 이 명령 종류를 보고 맞는 시계를 읽는다 --
> 자산·분류·태그·예산은 `assetClock`, 태그 표시는 `latestEntryHlc`, 나머지 전표는
> `entryHlc` 다.
>
> 종류에서 표를 얻는 일은 `settingTableOf()` 하나로 모았다. 쌓는 쪽
> (`local-settings-writer`)이 call site 마다 표 이름을 적어 넘기고 있어서 다시 내는 쪽과
> 갈라진 것이 이 버그의 뿌리였다. 이제 양쪽이 같은 함수를 본다.

**증상.** 보류 칸에서 "다시 보내기"를 누르면 거래는 이기지만, 통장 이름·분류·태그·예산의 충돌은
같은 자리에서 또 충돌한다. 사용자는 몇 번을 눌러도 자기 값이 들어가지 않는 것을 본다.

**근거.** `local-store.ts:2096` — 재시도는 새 시계를 뽑을 때 `entryHlc(targets[0])` 만 본다.
전표 표에서 읽는 함수라 대상이 통장·분류·태그·예산이면 언제나 `null` 이고, 그러면 기기의 물리
시계만으로 번호를 매긴다. 서버의 `fieldHlc` 가 더 앞서 있으면 `mergeFields` 가 또 밀어낸다.
정상 쓰기 경로는 `local-settings-writer.ts:76` 에서 `store.assetClock(table, id)` 를 제대로 보는데,
재시도만 그 갈래를 타지 않는다.

**고칠 방향.** 재시도가 명령 종류로 갈라 `entryHlc` 와 `assetClock` 중 맞는 것을 읽게 한다.
아웃박스 행에 `kind` 가 이미 있으므로 분기 하나면 된다.

### D. 지워진 거래를 고친 명령은 되살릴 길이 없다 (중간) — 완료

> **고친 내용.** 보류 칸에 **"새 거래로 다시 적기"** 를 두었다. 고치려던 거래가 사본에서
> 사라졌을 때만 나오고(`HeldMutation.targetMissing`), 그때는 다시 보내기 대신 이것이 선다 --
> 다시 보내 봐야 없는 거래를 고치려는 것이라 영영 거절되기 때문이다.
>
> 되살리지 않고 **새로 적는다.** 지운 사람의 뜻을 뒤집지 않으면서 적은 내용도 잃지 않는다.
> 만들기 경로를 그대로 다시 쓰므로(`entryWritePort().createEntry`) 조립·검증·사본 쓰기가
> 평소와 같은 길로 간다. 만들기를 먼저 하고 큐에서 뺀다 -- 순서를 뒤집으면 그 사이에 앱이
> 죽었을 때 적은 것이 사라진다.

**증상.** 오프라인에서 고친 거래를 그 사이 다른 사람이 지웠다면, 그 명령은 `ENTRY_NOT_FOUND` 로
거절되어 보류 칸에 남는다. 사용자가 할 수 있는 것은 "다시 보내기"(또 거절된다)와 "버리기"뿐이다.
오프라인에서 적은 내용이 **결국 사라진다.**

**근거.** `mutation-replay.service.ts:426-433` 이 거절하고, `OutboxScreen` 이 재시도·버리기만
제공한다. 재시도는 `entry.replace` 를 그대로 다시 낸다.

**고칠 방향.** 대상이 사라진 `entry.replace` 는 보류 칸에서 **"새 거래로 다시 적기"** 를 제공한다.
`entry.create` 로 종류를 바꾸고 새 id 를 주면 그대로 나간다. 삭제가 이기는 규칙은 그대로 두되,
사람이 잃은 것을 되살릴 문은 열어 두는 것이다.

### E. 태그를 지워도 사본의 연결이 남는다 (중간) — 완료

> **고친 내용.** 자리표 처리에 한 줄 더했다. 태그가 사라지면 그 태그를 가리키던
> `entry_tag` 도 함께 지운다 (서버의 cascade 와 같은 규칙).

**증상.** 사본에 죽은 `entry_tag` 행이 쌓인다. 화면에는 드러나지 않는다 — 태그를 읽는 질의가
`JOIN tag`(내부 조인, local-store.ts:1255)라 조용히 걸러지기 때문이다. 다만 그 태그 id 를
나중에 다른 태그가 재사용하거나 별칭(`applyAlias`)이 그 id 로 옮겨 오면 엉뚱한 태그가 붙는다.

**근거.** `local-store.ts:701-726` — 자리표를 적용할 때 `entry` 는 다리와 할부를, `budget` 은 월
조정을, `account` 는 평가액을 함께 지우는데 **`tag` 만 딸린 연결을 지우지 않는다.** 서버 쪽에는
FK cascade 가 있어(migrations/20260903150000_tags) 문제가 없고, 사본에만 남는다.

**고칠 방향.** 자리표 처리에 한 줄 더한다: `if (table === 'tag') DELETE FROM entry_tag WHERE tagId = ?`.

### F. 숨기기 선행조건을 트랜잭션 밖에서 읽는다 (중간) — 완료

> **고친 내용.** 넷(통장·카드·구성원·분류) 모두 확인과 숨기기를 한 트랜잭션에 넣고, 그
> 첫 문장에서 A 와 **같은 자문 잠금**을 잡는다(`common/ledger-lock.ts`).
>
> 잠그는 것만으로는 모자라서 **전표를 만드는 길도 같은 잠금을 잡게 했다**(`createEntry`).
> 스스로 읽고 고칠 것은 없지만 그 쓰기가 남의 선행조건을 깨기 때문이다 -- 확인하는 쪽만
> 잠그면 아무 소용이 없다.

**증상.** 잔액이 남은 통장, 미결제액이 남은 카드, 통장을 가진 구성원이 숨겨질 수 있다. 화면에서
사라지는데 돈은 남아 있어, 총자산과 목록이 어긋난다.

**재현(추정).** A 가 잔액 0 인 통장을 숨기는 순간 B 가 그 통장으로 지출을 적는다. A 의 검사는
이미 통과했고 트랜잭션 밖이라 B 의 쓰기를 막지 못한다.

**근거.**
- `accounts.service.ts:379,386` — 카드 수와 잔액을 `this.prisma` 로 읽고, 그 뒤 별도 UPDATE 를 낸다.
- `cards.service.ts:312` — 미결제액을 트랜잭션 **밖에서** 읽고 305행에서 읽은 값을 316행 트랜잭션이 쓴다.
- `people.service.ts:155` — 주인인 통장 수도 같다.
- `categories.service.ts:210` — 이 분류를 쓰는 다리 수도 같다(218행 트랜잭션 밖).

**고칠 방향.** 검사와 쓰기를 한 트랜잭션에 넣고, 검사 대상 행을 잠근 뒤 읽는다. 프로젝트 행
잠금(항목 A 의 2번)을 도입하면 이 넷이 함께 해결된다.

### G. 같은 태그를 동시에 붙이면 500 이 난다 (중간) — 완료

> **고친 내용.** `createMany` 에 `skipDuplicates: true` 를 주었다. 둘이 바란 결과가 같으므로
> 오류가 아니라 같은 상태에 닿는 것이 옳다.

**증상.** 두 사람이 같은 거래에 같은 태그를 동시에 붙이면 늦은 쪽이 서버 오류를 본다.

**근거.** `entries.service.ts:211` — `tx.entryTag.createMany({ data: rows })` 에 `skipDuplicates` 가
없다. 지금 붙은 태그를 읽는 206행 위쪽 질의는 잠그지 않으므로, 두 트랜잭션이 모두 "아직 없다"로
읽고 둘 다 INSERT 한다. `EntryTag` 의 기본키가 (entryId, tagId) 라 늦은 쪽이 유일 제약 위반으로
터진다. 재생 경로는 `mutation-replay` 가 모든 예외를 잡아 거절로 바꾸므로 500 대신 보류로 간다 —
**온라인에서 누른 사람만 오류를 본다.**

**고칠 방향.** `skipDuplicates: true` 를 준다. 결과가 같아지는 쪽이 옳다 — 둘 다 "이 태그를 붙여라"
라고 말했고 결과도 그것이다.

### H. 분류 숨기기가 시계를 찍지 않는다 (중간) — 완료

> **고친 내용.** `deleteCategory` 가 `hlc` 를 받아 자신과 함께 내려가는 소분류에 각각
> 시계를 찍는다. 소분류를 `updateMany` 로 한 번에 고치던 것을 줄마다 고치는 것으로 바꿨다 --
> 필드별 시계는 그 행이 지금 들고 있는 값 위에 얹는 것이라 행마다 다르기 때문이다.
>
> **덤으로 구멍 하나를 더 막았다.** 재생 경로가 분류의 `isActive:false` 를 `updateCategory`
> 로 직접 넣고 있어서 `CATEGORY_IN_USE` 검사를 통째로 건너뛰었다. 나머지 넷처럼
> `deleteCategory` 를 거치게 바꿨다.

**증상.** 온라인에서 숨긴 분류를, 그보다 **앞선** 오프라인 편집이 되살릴 수 있다.

**근거.** `categories.service.ts:218-226` — 다른 넷(구성원 `people:166`, 통장 `accounts:397`,
카드 `cards:327`, 태그 `tags:154`)은 전부 `stampFieldClocks(..., ['isActive'], ...)` 를 찍는데
분류만 찍지 않는다. 대분류를 지울 때 함께 내리는 소분류(220행 `updateMany`)도 마찬가지다.
`mergeFields` 는 시계가 없는 필드를 "다툰 적 없다"로 보고 통과시키므로, 재생되는 옛
`category.update { isActive: true }` 가 이긴다.

**고칠 방향.** 나머지 넷과 같은 모양으로 시계를 찍는다. `deleteCategory` 가 `hlc` 인자를 받지
않는 것도 함께 맞춘다(재생이 부를 자리가 생긴다).

### I. 권한이 사라진 기기가 아무 말 없이 멈춘다 (중간) — 완료

> **고친 내용.** 셋 다 처리했다.
>
> 1·2. 동기화가 `PROJECT_FORBIDDEN`(내보내기·탈퇴·프로젝트 삭제)을 받으면 사본을 버리고
>    화면에 알린다. **맨 403 으로 판단하지 않는다** -- viewer 로 내려간 기기도 명령을 올릴 때
>    403 을 받는데, 그쪽은 아직 구성원이라 사본을 지우면 볼 수 있는 것까지 잃는다.
>    버리는 일은 그 동기화가 끝난 뒤에 한다(`clearOffline` 이 돌고 있는 동기화를 기다리므로
>    안에서 부르면 서로 멈춘다).
> 3. `useCanEdit()` 을 두고 웹·앱의 쓰기 단추를 감춘다. 공용 자리에 걸어 한 번에 여럿을
>    덮었다 -- `AddButton`(구성원·통장·카드·분류·태그 추가), 앱의 `DragList`(끌기와 줄 누르기),
>    거래 추가·고치기, 상세 팝업의 고치기·지우기, 거래 화면의 더보기(태그·삭제).
>    목록과 상세는 그대로 읽힌다.

**증상 셋.**

1. 프로젝트에서 내보내진 기기는 pull 이 403 을 받는다. `offline.ts:163` 이 콘솔에만 적고 `null` 을
   돌려주므로 화면에는 아무 표시가 없다. 사용자는 며칠 지난 사본을 최신으로 믿는다.
2. 그 사본에는 **남의 가계부가 통째로 남는다.** `clearLocalMirror` 는 로그아웃과 사용자 교체에서만
   부른다(mirror-teardown.ts). 내보내기는 그 자리에 들지 않는다.
3. editor 에서 viewer 로 내려간 기기는 계속 쓸 수 있다. 화면에 역할 검사가 없고
   (`packages/app/src/screens/*` 어디에도 `role === 'viewer'` 로 쓰기를 막는 곳이 없다), 쌓인 명령은
   push 에서 전부 거절되어 보류 칸에 쌓인다. 서버가 막으므로 데이터는 안전하지만 사용자 경험이 나쁘다.

**고칠 방향.** pull·push 의 403 을 오프라인 오류와 갈라 다룬다. "이 프로젝트에 더 접근할 수
없습니다"를 화면에 띄우고, 아직 보내지 못한 명령을 내보낼 길을 준 다음 그 프로젝트의 사본을
버린다. 역할은 `useProjectMembership` 이 이미 알고 있으므로 쓰기 버튼을 가리는 것은 어렵지 않다.

### J. 프로젝트가 지워지면 사본에 유령이 남는다 (낮음) — 완료

> **고친 내용.** I 와 한 경로다. 프로젝트가 지워지면 구성원 행도 함께 사라져 같은
> `PROJECT_FORBIDDEN` 이 오고, 사본을 버리고 목록에서 뺀다.

`sync_tombstone` 은 프로젝트가 사라지는 중이면 자리표를 남기지 않는다(의도된 설계다 — 볼 기기가
없다고 본다). 하지만 다른 구성원의 기기에는 그 프로젝트의 사본이 그대로 있고, pull 은 404 로
끝난다. 항목 I 와 같은 자리에서 함께 처리하면 된다.

### K. 하드 삭제가 전표 번호를 올리지 않는다 (낮음, 기존에 문서화됨) — 완료

> **고친 내용.** `EntryTag` 에 AFTER DELETE 트리거를 걸었다
> (`20260907090000_entry_tag_hard_delete`). **태그가 이미 사라진 경우에만** 전표의
> `updatedAt` 을 건드려 도장을 깨운다 -- 평소의 태그 변경(전표 저장마다 연결 수십 줄이
> 지워진다)에는 아무 일도 하지 않아야 그 비용이 붙지 않는다.
>
> 시계(`updatedHlc`)는 올리지 않는다. 사람의 편집이 아니라 뒷정리라, 올리면 그 사이
> 도착한 오프라인 편집이 진 것처럼 밀린다.
>
> **분류의 하드 삭제는 그대로 두었다.** `Posting.category` 가 cascade 라 분류를 지우면
> 번호가 아니라 **다리가 통째로 사라진다** -- 동기화의 문제가 아니라 그보다 큰 사고이고,
> 막는 것은 서비스의 `CATEGORY_IN_USE` 다. FK 를 restrict 로 바꾸면 프로젝트 삭제가
> 막히므로 여기서 건드릴 일이 아니다.

태그를 손으로 지우면 `EntryTag` 만 cascade 로 사라지고 전표의 `updatedVersion` 은 그대로라, 기기가
그 변화를 영영 받지 못한다. `migrations/20260903150000_tags` 의 주석이 이미 짚어 두었다. 화면은
소프트 삭제만 하므로 지금은 닿지 않는 길이다. 정리 스크립트를 새로 쓸 때 잊기 쉬우니 남겨 둔다.

분류를 하드 삭제하면 사본의 지출 집계 질의(`local-store.ts:924` `JOIN category`)가 그 다리를
조용히 빼므로 목록 합계와 보고서 합계가 갈린다. 같은 갈래다.

### L. 요청 문맥 밖의 쓰기는 신호를 내지 않는다 (낮음) — 하지 않음

> **결정.** 지금은 서버가 스스로 쓰는 작업을 늘릴 계획이 없어 그대로 둔다 (2026-09-07).
> 정확성은 상하지 않는다 -- 번호는 오르므로 다음 pull 이 커서로 따라잡고, 늦어질 뿐이다.
> 배치 쓰기를 늘리게 되면 그 스크립트 끝에서 `SyncEventsService.publish` 를 부르면 된다.

`recordProjectWrite` 는 AsyncLocalStorage 를 쓰고, 그 문맥은 `SyncNotifyMiddleware` 가 HTTP 요청에만
연다. 배치·크론·수동 스크립트(예: 환율 갱신, `prune-tombstones`)가 데이터를 바꾸면 번호는 오르지만
**신호가 나가지 않는다.** 열어 둔 화면은 다음 pull 까지 모른다. 정확성은 상하지 않으므로 우선순위는
낮지만, 앞으로 서버 쪽 자동 쓰기를 늘린다면 스크립트가 끝날 때 `SyncEventsService.publish` 를
직접 부르는 자리를 하나 두어야 한다.

---

## 3. 검증 공백

- `scripts/convergence-smoke.ts` 는 **순수 함수**(mergeFields·applyTagChange·rank)만 무작위로
  흔든다. 좋은 검사지만 데이터베이스를 쓰지 않아 트랜잭션 경합은 잡지 못한다.
- **메웠다.** `scripts/entry-concurrency-smoke.ts` 가 요청을 겹쳐 던진다. 잔액과 다리 합계의
  일치, `baseHlc` 판정, 같은 태그 동시 부착, 숨기기 선행조건과 시계까지 21건을 본다.
  착수 전에 잠금을 뺀 채로 돌려 실패를 먼저 확인했다 -- 잔액이 사라진 중간 버전의 금액만큼
  어긋난다는 추론이 그대로 재현됐다.
- **점검 도구.** `scripts/check-balances.ts` 는 읽기만 하고 어긋난 계좌를 찾는다. 실제로
  개발 데이터베이스에서 한 건을 찾았다.
- 아직 없는 것: **기기 사본(LocalStore)을 도는 검사.** 항목 C 의 시계 선택은
  `settingTableOf` 의 대응표만 손으로 확인했고(16종 전부), D 의 "새 거래로 다시 적기"와
  I 의 사본 버리기는 코드를 읽어 확인했다. node 에서 sqlite 를 열 채비가 없어 그 흐름
  전체를 돌려 보지는 못했다. **앱에서 손으로 한 번 밟아 보는 것이 남았다.**
- HTTP 스모크 7개(`auth`·`currency`·`permissions` 등)는 서버가 떠 있어야 돈다. 이번 작업
  전후로 똑같이 `fetch failed` 다.

---

## 4. 남은 일

코드에서 고칠 것은 끝났다. 남은 것은 사람이 확인할 일이다.

1. **운영 데이터베이스에 `check-balances.ts` 를 돌린다.** 개발 쪽에서 한 건이 나왔으므로
   운영에도 있을 수 있다. 어긋난 계좌가 나오면 그 계좌의 거래를 먼저 훑고, 맞다고
   판단했을 때만 스크립트가 적어 주는 UPDATE 를 돌린다.
2. **앱에서 두 흐름을 손으로 밟아 본다.** 보류 칸의 "새 거래로 다시 적기"(D)와, 내보내진
   기기가 사본을 버리는 자리(I). 둘 다 자동 검사가 없다.
3. **배포 순서.** 서버를 먼저 올린다. 옛 화면은 `baseHlc` 를 보내지 않아 지금까지처럼
   동작하고, 새 화면이 옛 서버에 붙으면 그 값이 무시될 뿐이다 -- 어느 쪽 순서로도 깨지지
   않지만, 서버가 먼저 올라가야 보호가 하루라도 빨리 걸린다.
